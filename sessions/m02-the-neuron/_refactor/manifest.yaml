# =============================================================================
# Module 2 Refactor Manifest — "The Neuron & How It Learns"
# =============================================================================
# Durable source of truth for the m02 refactor. Reconciles the v7.2 loop
# (QA + fix + held-out eval + skills patch) and applies the v7.3-patched skills
# (S1-S4) as a report-only DELTA QA against sessions/m02-the-neuron.
#
# Owner skill: frontier-curriculum-architect (manifest rule)
# QA skill:    frontier-refactor-qa (manifest update gate)
# This file is authoritative. Do not leave findings only in prose reports.
# =============================================================================

meta:
  module: "m02-the-neuron"
  title: "The Neuron & How It Learns"
  path: "sessions/m02-the-neuron"
  lessons: 9            # day-01 .. day-09
  gates: ["review-part-a.html", "review.html"]  # mid-module (M2 half) + end-module
  merged: "Module 2 and Module 3 merged; learner-facing labels unified as Module 2"
  status: "pass_with_p1"
  seed_module: true     # ready as the seed module for next-module rollout
  gate_bar: "0 P0"
  open_p0_count: 0
  last_loop: "v8-phase-d-source-all-9-days"
  last_updated: "2026-07-09"
  learning_experience_gate: "PASS — v7.6 LX stabilization applied to ALL 9 lessons (D1-D2 seed + D3-D9 propagation sweep); see loop_history v7.6"

# -----------------------------------------------------------------------------
# V8 PHASE B — SOURCE-FIRST COMPILER PILOT  (day-01 only)
# -----------------------------------------------------------------------------
# Day 1 is now authored source-first: sessions/.../day-01-single-neuron/source.md
# is the canonical source; lesson.html is COMPILED by sessions/_compiler/compile_lesson.py.
# Day 2-9 remain hand-authored (v7.6). This pilot proves the compiler path only.
v8_phase_b_pilot:
  status: completed
  date: "2026-07-08"
  scope: "day-01-single-neuron only"
  source: "sessions/m02-the-neuron/day-01-single-neuron/source.md"
  compiler: "sessions/_compiler/compile_lesson.py"
  donor: "sessions/_compiler/shells/m02-day-01.donor (pristine v7.6 shell snapshot)"
  v7_6_backup: "sessions/_compiler/shells/m02-day-01.v7_6-backup"
  report: "sessions/_refactor/v8_phase_b_m02_day1_pilot_report.md"
  authored_change: >
    Reader-flow improvement to the FIRST SCREEN only: hero reopened human-intuition-first
    ("your brain is weighing signals...") instead of the v7.6 frontier-first GPT-3 opening;
    added a front-loaded Jargon Ladder table in s1; GPT-3/frontier payoff moved to s4 (after
    mechanism). Mechanism, staff-lens, artifact, and all anchors unchanged.
  determinism: "compile run twice -> byte-identical output"
  shell_invariant: "CSS block + all 4 <script> blocks (incl. 19,299-char JS engine w/ DEMOS/BUILD/QS) byte-identical to donor; only content regions (hero/s1/s2/s4/s7) differ"
  gates:
    reader_flow: "PASS (9/9 source checks: human-first hero, jargon ladder, picture-before-vocab, frontier deferred to s4, spine hero+s1+s2, discovery produce)"
    shell_invariant: "PASS (quest-id frozen, 7 sections, 8 data-targets, DEMOS/BUILD/QS present, playground>=3, quiz q:4 o:16, localStorage keys, .fin, nav)"
    notebook_smoothness: "PASS (pilot/exemplar) — first-screen barrier now matches the notebook's human-first + jargon-buster opening; beats v7.6 (which opened frontier-first)"
  audits:
    lesson_audit: "9 OK / 0 MISSING / 0 LEFTOVER / 0 DEGRADED"
    nav_audit: "PASS — 0 CASE, 0 BROKEN, 0 orphans (donor/backup stored without .html ext so the audit skips them)"
    staff_lens_audit: "9/9 staff-lens present, gap 0, day-01 q:4 o:16 errs:[] (render:BROKEN benign)"
    node_check: "all 4 inline scripts parse"
  frozen_invariants_verified: "quest-id wf2-d01-neuron; data-quest-id; nav prev/next; localStorage frontier-lesson:<qid>+frontier-theme; quiz q:4 o:16; BUILD/DEMOS behavior; .fin completion; experiment.py + log.md untouched"
  open_after_pilot: "P1/P2 only (documented in report §7): s2/s4/s7 still use raw-HTML escapes for complex widgets; d02-d09 not yet sourced; behavioral-viz P1s (BL-VE-D1/D2) unchanged."

# -----------------------------------------------------------------------------
# V8 PHASE C — COMPILER HARDENING  (day-01 recompiled; reusable for day 02-09)
# -----------------------------------------------------------------------------
v8_phase_c_hardening:
  status: completed
  date: "2026-07-08"
  report: "sessions/_refactor/v8_phase_c_compiler_hardening_report.md"
  refactor:
    - "compiler core -> sessions/_compiler/v8lib.py (parsing, markdown-lite, typed widgets, region compile)"
    - "gates extracted -> sessions/_compiler/gates/reader_flow_gate.py + shell_invariant_gate.py (standalone CLI + importable run())"
    - "sessions/_compiler/compile_lesson.py is now a thin orchestrator importing v8lib + both gates"
  typed_widgets: ["jargon", "table", "cards", "formula", "mathladder", "prompt"]
  raw_html_escapes_remaining_in_day01: 0   # all 5 complex widgets converted to typed blocks
  determinism: "compile twice -> byte-identical"
  shell_invariant: "CSS + all 4 <script> byte-identical to donor (gate now asserts this automatically with --donor)"
  audits: "lesson_audit 9 OK / 0 advisories · nav PASS · staff-lens 9/9 gap 0 (day-01 fail:1 trade:1 q:4 o:16 errs:[]) · node --check all parse · 11/11 widget spot-checks OK"
  reusable_for_d2_d9: "yes — v8lib + typed-block vocabulary + standalone gates are lesson-agnostic; a new day needs only a source.md + a donor snapshot"
  open_after_phase_c: "P1/P2 only — day 02-09 not yet sourced (Phase D); ~~~html raw escape retained as a fallback for one-off widgets not yet typed; behavioral-viz P1s (BL-VE-D1/D2) unchanged."

# -----------------------------------------------------------------------------
# V8 PHASE D — SOURCE ALL 9 DAYS + NOTEBOOK SMOOTHNESS GATE
# -----------------------------------------------------------------------------
v8_phase_d_sourcing:
  status: completed
  date: "2026-07-09"
  report: "sessions/_refactor/v8_phase_d_sourcing_report.md"
  all_9_sourced: true
  sourcing_mode:
    day-01: "clean typed-block (Phase B/C exemplar)"
    day-02..09: "verbatim/migration mode via extract_source.py — regions captured byte-for-byte; round-trip byte-identical to shipped v7.6 lesson (zero content regression; D2-09 lesson.html unchanged in git)"
  donors: "sessions/_compiler/shells/m02-day-0N.donor for N=01..09 (stored without .html so nav_audit skips them)"
  notebook_smoothness_gate:
    result: "PASS"
    breakdown: "6 PASS (D1-D6 vs fundamentals 01/03/05/06/07/08), 3 N/A (D7-D9 no matching notebook -> skipped per No-Notebook rule), 0 FAIL"
    gate: "sessions/_compiler/gates/notebook_smoothness_gate.py"
  per_day_gates: "all 9: Reader Flow exit0 (D1 strict PASS; D2-09 informational), Shell Invariant PASS (CSS+4 scripts byte-identical to donor), deterministic (compile x2 -> identical)"
  audits: "lesson_audit 9 OK/0 advisories · nav PASS · staff-lens 9/9 gap 0"
  open_after_phase_d: "P1/P2 only — day-02..09 are verbatim (not yet clean typed-block: future per-day authoring); s3/s5/s6 scaffolding carried from donor; ~~~html fallback retained; behavioral-viz P1s (BL-VE-D1/D2) unchanged. No P0."


# -----------------------------------------------------------------------------
# STATUS SUMMARY (top-line, human-readable)
# -----------------------------------------------------------------------------
status_summary: >
  Module 2 is CLEAN at the P0 bar. v7.2 independent audit: 0 P0 across 9 lessons
  + 2 gate files; 32/33 must-cover rows PASS (1 contract-waived to P2); all six
  gates PASS; Anchor Consistency no-P0; all defect classes closed. Held-out eval
  found 4 SYSTEMIC skill gaps (S1-S4) that are NOT module-P0s; they were patched
  into the frontier-* skills (forward-looking). Applying the v7.3-patched skills
  as a retroactive DELTA QA against m02 surfaces evidence-medium upgrade items
  (plotted derivatives, live boundary/geometry viz, XOR limit, interview
  grounding) — all classified P1 under grandfathering (would be P0 for a fresh
  v7.3 build). No new module P0. Status: pass_with_p1. Ready as seed module.

# -----------------------------------------------------------------------------
# CONSTRAINTS  (frozen invariants + scope rules — never violate)
# -----------------------------------------------------------------------------
constraints:
  frozen_invariants:
    - id: quest_ids
      value: ["wf2-d01-neuron", "wf2-d02-activations", "wf2-d03-forward",
              "wf3-d01-loss", "wf3-d02-backprop", "wf3-d03-training-loop",
              "wf3-d04-optimizers", "wf3-d05-lr", "wf3-d06-split",
              "wf2-review", "wf3-review"]
      note: "data-quest-id scheme FROZEN. wf2/wf3 prefixes reflect the M2+M3 merge; do not renumber."
      verified: "grep confirmed all 9 lesson quest-ids intact (v7.3 delta QA)"
    - id: interactive_arrays
      value: ["BUILD", "DEMOS", "QS"]
      note: "Frozen. Playground >=3 gate + quiz answered gate; every day q:4 o:16."
    - id: completion_wiring
      value: ["frontier-lesson:<qid> localStorage keys", "nav data-target links", ".fin completion banners"]
      note: "Untouched. Do not alter completion behavior."
    - id: shared_shell
      note: "Shared shell files (sidebar + Appearance switcher, 4 themes, dark default) untouched."
  scope_rules:
    - "Do NOT edit lesson files during QA/eval/manifest loops (report-only)."
    - "Source-free: do not read old notebooks unless verifying an existing held-out-eval claim."
    - "Phase 1 fixes P0 only; P1/P2 out of the fix-P0-only scope."
  sanctioned_designs:  # explicitly NOT defects
    - id: guided_stub_experiment_py
      note: "experiment.py per day is a deliberate 5-line guided stub (m02_artifact_contract.md §31: 'intentionally NOT a filled reference solution — the learner produces it'). NOT a defect. Do not re-flag as P0."
    - id: dead_experiment_lab
      note: "/frontier-experiment-lab is uninstalled; referenced in every Produce Option-B + stub. Curriculum-wide (~243 files) P1. Never fix per-module. S2 dead-escape-hatch flag now surfaces it (>=P1) instead of silently waiving."
    - id: static_svg_structural
      note: "Static SVGs are acceptable for STRUCTURAL diagrams. Do not re-flag structural SVGs as P0. Only BEHAVIORAL concepts need plotted/live evidence (S1)."
    - id: bilingual_light_touch
      note: "English-primary, light-touch bilingual (user preference overrides frontier default). Mid-lesson Chinese callouts on D1 are a P2 nit, not systemic."
    - id: staff_lens_render_benign
      note: "staff_lens_audit.js render:BROKEN / secs:0 = known-benign .sec vs .module-section selector mismatch. Not a regression."
  skills_state:
    installed: ".claude/skills/frontier-*/SKILL.md"
    mirror: "frontier_lab_refactor_skills_v7_6/skills/*"
    invariant: "installed == mirror (byte-identical)"
    version: "v7.6 (adds Autonomous Rollout Loop + Seed Propagation Risk + Seed Stabilization Phase over the v7.5 Learning Experience Gate; all additive, no rule removed)"
    verified: "diff -q on all 4 SKILL.md vs frontier_lab_refactor_skills_v7_6/skills -> IDENTICAL (2026-07-08)"

# -----------------------------------------------------------------------------
# QUALITY GATES  (v7.2 result + v7.3 delta-QA result)
# -----------------------------------------------------------------------------
quality_gates:
  - gate: "Coverage traceability (33 must-cover)"
    v7_2: "PASS — 32/33 PASS, 0 FAIL, 1 WAIVED (row 12 -> P2)"
    v7_3_delta: "PASS — no must-cover downgraded; boundary-geometry evidence upgrade tracked as P1 (VE-D1)"
  - gate: "Coach Voice"
    v7_2: "PASS 9/9 (term tooltips, Math Ladder, 1 silent-failure + 1 trade-off callout, interview answer, diagnostic quiz Q)"
    v7_3_delta: "PASS"
  - gate: "Foundation Framing (D1)"
    v7_2: "PASS — all 5 elements (bio intuition, artificial caveat, mapping table, analogy-breaks, transition to activation(w·x+b))"
    v7_3_delta: "PASS"
  - gate: "Function Family (D2)"
    v7_2: "PASS — ReLU/sigmoid/tanh full; Leaky/GELU/SwiGLU aware; softmax deferred to D4; derivative intuition + saturation + vanishing + dying ReLU"
    v7_3_delta: "PASS coverage; behavioral-derivative VISUAL upgrade tracked as P1 (VE-D2)"
  - gate: "Visual / Evidence"
    v7_2: "PASS 9/9 — every P0 visual shows a changing quantity; D5/D7/D8 live gradient-descent.html embeds auto-size"
    v7_3_delta: "PASS_WITH_P1 — under S1 behavioral-visual rule, D1 boundary geometry + D2 derivatives + saturation/dead-ReLU are prose/static-SVG only. Grandfathered to P1 (contract-scoped static; module proves live-viz capability on D5/D7/D8)."
  - gate: "Anchor Consistency"
    v7_2: "NO P0 — playground == Math Ladder == Produce acceptance numbers on every day; 2 residual P1 (D8 5/5/4 vs 8/8/8; D9 axis framing) teach no conflicting model"
    v7_3_delta: "NO P0"
  - gate: "Artifact"
    v7_2: "PASS 9/9 — Produce prompts + acceptance numbers match artifact contract; run paths match folders; guided stubs per contract §31"
    v7_3_delta: "PASS"
  - gate: "Module-label identity (defect class #1)"
    v7_2: "CLOSED — 0 learner-facing self-leaks; M40 is an SVG path coord; review.html forward-pointers to Module 3 (Attention) correct"
    v7_3_delta: "CLOSED"
  - gate: "Manifest Update Gate"
    v7_2: "N/A (manifest did not yet exist)"
    v7_3_delta: "PASS — this loop creates the manifest; every P0/P1/P2 mapped to backlog; skill gaps -> skill_gap_candidates; reports -> loop_history"
  - gate: "Learning Experience Gate (v7.5) — Experience -> Mechanism -> Staff layering"
    v7_5_audit: "PASS_WITH_P1 (report-only) — 5 seed-propagation LX P1: picture-order (D1), opening-checklist (D1), formula-first (D2), narrative-spine (D2), artifact-as-discovery (D1+D2). Ref: sessions/m02_v7_5_learning_experience_audit.md"
    v7_6_stabilization: "PASS — LX gate now clean across ALL 9 lessons. D1-D2 seed (5 LX P1) resolved first; then D3-D9 propagation sweep applied the same targeted reorder+reframe (picture-before-vocabulary, curiosity opening, analogy spine into staff-lens, discovery Produce; D5/D8 also deferred an in-section formula). 0 P0; quest-ids/BUILD/DEMOS/QS/anchors verified intact on all 9. Ref: sessions/m02_v7_6_seed_stabilization_report.md"

# -----------------------------------------------------------------------------
# COVERAGE ITEMS  (33 must-cover concepts; from m02_coverage_contract.md)
# -----------------------------------------------------------------------------
coverage_items:
  must_cover:
    - {id: C01, concept: "Neuron as affine map z=w·x+b", depth: Core, lesson: D1, result: PASS}
    - {id: C02, concept: "Weights/bias are learned state; w orients, b shifts", depth: Core, lesson: D1, result: PASS}
    - {id: C03, concept: "Bias not optional (no b -> boundary through origin)", depth: Support, lesson: D1, result: PASS}
    - {id: C04, concept: "Symmetry-breaking initialization", depth: "Core (staff)", lesson: D1, result: PASS}
    - {id: C05, concept: "Nonlinearity required: linear∘linear collapses", depth: Core, lesson: D2, result: PASS}
    - {id: C06, concept: "ReLU/sigmoid/tanh formulas, ranges, ReLU default", depth: Core, lesson: D2, result: PASS}
    - {id: C07, concept: "Saturation + dead-ReLU counter-failure", depth: "Core (staff)", lesson: D2, result: PASS, note: "visual upgrade -> VE-D2 (P1)"}
    - {id: C08, concept: "Layer Z=xW+b with explicit shapes", depth: Core, lesson: D3, result: PASS}
    - {id: C09, concept: "Forward pass composing layers -> ŷ; batch; one numeric pass", depth: Core, lesson: D3, result: PASS}
    - {id: C10, concept: "Logits / raw scores", depth: Support, lesson: "D3->D4", result: PASS}
    - {id: C11, concept: "MSE — formula, why squared", depth: Core, lesson: D4, result: PASS}
    - {id: C12, concept: "MSE convex bowl in ŷ", depth: Core, lesson: D4, result: "WAIVED->P2", note: "curve present; bowl-in-ŷ not drawn; contract-waived"}
    - {id: C13, concept: "Cross-entropy (+softmax/sigmoid) & why not MSE", depth: Core, lesson: D4, result: PASS}
    - {id: C14, concept: "MSE vs CE comparison TABLE", depth: Core, lesson: D4, result: PASS}
    - {id: C15, concept: "Loss is a mean over the batch", depth: Support, lesson: D4, result: PASS}
    - {id: C16, concept: "Loss as scalar L(θ) — the surface", depth: Core, lesson: "D4->D5", result: PASS}
    - {id: C17, concept: "Gradient = partials uphill; −∇L descends", depth: Core, lesson: D5, result: PASS}
    - {id: C18, concept: "Vanilla GD named baseline (w<-w−η·dL/dw)", depth: Core, lesson: D5, result: PASS}
    - {id: C19, concept: "Backprop = chain rule backward, real numeric chain", depth: Core, lesson: D5, result: PASS}
    - {id: C20, concept: "Forward-cache -> train mem > inference", depth: Support, lesson: D5, result: PASS}
    - {id: C21, concept: "Non-convexity / local vs global min", depth: "Core (aware)", lesson: D5, result: PASS}
    - {id: C22, concept: "Training loop: fwd->loss->bwd->zero_grad->update", depth: Core, lesson: D6, result: PASS}
    - {id: C23, concept: "Epoch / iteration / batch (100/25=4)", depth: Support, lesson: D6, result: PASS}
    - {id: C24, concept: "Batch vs mini-batch vs SGD — noisy unbiased estimate", depth: Core, lesson: D6, result: PASS}
    - {id: C25, concept: "Random-init scale (fan-in)", depth: "Support (staff)", lesson: D6, result: PASS}
    - {id: C26, concept: "SGD update θ<-θ−η∇L stated exactly", depth: Core, lesson: D7, result: PASS}
    - {id: C27, concept: "Momentum rule (velocity) + what it fixes", depth: Core, lesson: D7, result: PASS, note: "g=−9.6, v=−24.0 verified"}
    - {id: C28, concept: "Adam rule (m,v, bias-corr, β1=.9/β2=.999/ε=1e-8)", depth: Core, lesson: D7, result: PASS}
    - {id: C29, concept: "LR as step size — small/right/large(->NaN)", depth: Core, lesson: D8, result: PASS}
    - {id: C30, concept: "LR schedules + warmup", depth: Support, lesson: D8, result: PASS}
    - {id: C31, concept: "Train / validation / test roles", depth: Core, lesson: D9, result: PASS}
    - {id: C32, concept: "Overfitting/underfitting via gap = val−train", depth: Core, lesson: D9, result: PASS}
    - {id: C33, concept: "Early stopping + regularization (L2/dropout)", depth: "Core (aware)", lesson: D9, result: PASS}
  coverage_gaps:  # surfaced by v7.3 S3 Capability-Limits & Geometry Lens
    - {id: CG1, concept: "Decision-boundary GEOMETRY (w=orientation, b=shift/threshold)", lesson: D1,
       state: "prose-only (D1:421 'shifts that line'); no picture/derivation", tier: "must-cover (evidence-medium)", backlog: BL-VE-D1}
    - {id: CG2, concept: "XOR / linear-separability — single-neuron capability LIMIT", lesson: "D1/D2",
       state: "absent (grep: 0 in D1 and D2)", tier: "Should-cover", backlog: BL-COV-XOR}
    - {id: CG3, concept: "Plotted derivatives of sigmoid/tanh/ReLU", lesson: D2,
       state: "prose 'slope near 0 ≈0.25' table row; 0 plotted derivatives (grep: derivative=0, svg=7 static)", tier: "must-cover (evidence-medium)", backlog: BL-VE-D2}
  result: "32/33 PASS · 0 FAIL · 1 WAIVED. Every must-cover lands at stated depth or is waived. Gaps CG1-CG3 are evidence-medium/should-cover, tracked as P1."

# -----------------------------------------------------------------------------
# VISUAL / EVIDENCE ITEMS  (per-day P0 visual + behavioral status under S1)
# -----------------------------------------------------------------------------
visual_evidence_items:
  per_day:
    - {id: VE-D1, lesson: D1, visual: "neuron build + playground (static SVG); build-embed CSS present but 0 iframe",
       behavioral_concept: "decision-boundary movement", v7_2: PASS, v7_3_delta: "P1 — behavioral, prose-only; needs plotted/live boundary viz", backlog: BL-VE-D1}
    - {id: VE-D2, lesson: D2, visual: "activation curves + tanh SVG + comparison table (7 static SVG)",
       behavioral_concept: "derivative behavior / saturation / dead-ReLU", v7_2: PASS, v7_3_delta: "P1 — derivatives + saturation are prose/static; needs plotted/live derivative curve", backlog: BL-VE-D2}
    - {id: VE-D3, lesson: D3, visual: "shape-chain build; concrete numeric forward pass [2,3]->[2,−3]->[2,0]->[2]", v7_2: PASS, v7_3_delta: PASS,
       note: "O3 (xW1)W2=x(W1W2) asserted in 3 places, shown in 0 in D3 — collapse IS shown in D2; folded into VE-D2/coverage"}
    - {id: VE-D4, lesson: D4, visual: "pred->err->sq->mean build; falling-loss curve; MSE↔CE real <table> L413-421", v7_2: PASS, v7_3_delta: PASS}
    - {id: VE-D5, lesson: D5, visual: "slope-on-bowl build + LIVE gradient-descent.html iframe (auto-size verified)", v7_2: PASS, v7_3_delta: PASS}
    - {id: VE-D6, lesson: D6, visual: "loop-cycle + falling loss + batch boxes", v7_2: PASS, v7_3_delta: PASS}
    - {id: VE-D7, lesson: D7, visual: "optimizer paths + ball/momentum build + LIVE gradient-descent.html iframe", v7_2: PASS, v7_3_delta: PASS}
    - {id: VE-D8, lesson: D8, visual: "LR slider + curve + LIVE gradient-descent.html iframe (0.005 crawl / 0.1 conv / 0.6 diverge)", v7_2: PASS, v7_3_delta: PASS}
    - {id: VE-D9, lesson: D9, visual: "3-box split build + two-curve diverge + early-stop marker", v7_2: PASS, v7_3_delta: PASS}
  live_viz_capability:
    proven_on: ["D5", "D7", "D8"]   # gradient-descent.html iframe, postMessage auto-resize
    absent_on: ["D1", "D2", "D3", "D4", "D6", "D9"]  # 0 iframes (grep-verified)
    implication: "Module demonstrably CAN host live viz; D1/D2 behavioral upgrades are additive, not missing-infra — supports P1 (not P0) classification."

# -----------------------------------------------------------------------------
# ARTIFACT ITEMS  (Produce experiment.py per day)
# -----------------------------------------------------------------------------
artifact_items:
  design: "One Produce artifact/day = guided-stub experiment.py + empty log.md (contract §31, deliberate learn-by-building)."
  acceptance_numbers_verified: "Phase-1 agents recomputed D5/D6/D7/D8 anchors and confirmed them correct (not fabricated)."
  run_paths: "Every run command path == actual folder; every stub commented run path matches its folder (v7.2 PASS 9/9)."
  option_b_dead_path: "Every Produce Option-B routes through uninstalled /frontier-experiment-lab -> P1-legacy (curriculum-wide, never fix per-module)."
  five_minute_log: "Each lesson carries the 📓 5-minute-log callout with explain-back question."

# -----------------------------------------------------------------------------
# BACKLOG  (every P0/P1/P2 finding maps here)
# -----------------------------------------------------------------------------
backlog:
  p0: []   # OPEN P0 COUNT = 0

  p1:
    # --- carried from v7.2 QA report ---
    - {id: BL-P1a, lesson: D2, source: "v7.2 QA (P1-a)", status: open,
       finding: "Perceptron/XOR aware historical hook absent (Should-cover)", fix_shape: "+1 prose sentence in §1/§4"}
    - {id: BL-P1b, lesson: D7, source: "v7.2 QA (P1-b)", status: open,
       finding: "No SGD/momentum/Adam comparison TABLE (grep: 0 <table> in D7)", fix_shape: "add 3-row table in §4"}
    - {id: BL-P1c, lesson: D8, source: "v7.2 QA (P1-c)", status: open,
       finding: "Playground step counts 5/5/4 vs Produce 8/8/8 — narrated (day-08:390), no conflicting model", fix_shape: "extend playground rows to 8"}
    - {id: BL-P1d, lesson: D9, source: "v7.2 QA (P1-d)", status: open,
       finding: "Playground framing (steps-axis) != Produce framing (degree-axis) — bridged by capacity callout day-09:427", fix_shape: "+1 unifying sentence"}
    - {id: BL-P1e, lesson: review.html, source: "v7.2 QA (P1-e)", status: open,
       finding: "End-gate quiz + self-check cover D4-D6 only; copy claims 'whole module' (QS frozen)", fix_shape: "reword L171/L189"}
    - {id: BL-P1-legacy, lesson: "all 9", source: "v7.2 QA (P1-legacy) + S2 dead-escape-hatch flag", status: "wont_fix_per_module",
       finding: "/frontier-experiment-lab (uninstalled) in every Produce Option-B + stub", fix_shape: "Contract-sanctioned curriculum-wide (~243 files); fix globally when skill reinstated, never per-module"}
    # --- NEW: v7.3 delta QA (retroactive S1-S4 gates; grandfathered P1) ---
    - {id: BL-VE-D1, lesson: D1, source: "v7.3 delta (S1 behavioral + S3 geometry + S4 grounding)", status: open, priority: P1,
       finding: "Decision-boundary geometry is behavioral but prose-only (build-embed CSS present, 0 iframe); interview line D1:436 recites 'shifts the decision boundary' with no boundary ever drawn (S4 ungrounded)",
       fix_shape: "add one live viz/*.html boundary embed (w rotates line, b shifts it) OR cut the boundary phrase; grounds S1+S4 together",
       retro_note: "Would be P0 for a fresh v7.3 build; P1 for shipped m02 (contract-scoped static; teaches no wrong model)"}
    - {id: BL-VE-D2, lesson: D2, source: "v7.3 delta (S1 behavioral)", status: open, priority: P1,
       finding: "Activation derivatives + saturation + dead-ReLU are behavioral but shown via static SVG + prose 'slope≈0.25' table (grep: derivative=0, svg=7 static)",
       fix_shape: "add plotted/live derivative curves for sigmoid/tanh/ReLU showing slope flatten at the tails",
       retro_note: "Would be P0 for a fresh v7.3 build; P1 for shipped m02"}
    - {id: BL-COV-XOR, lesson: "D1/D2", source: "v7.3 delta (S3 Capability-Limits Lens)", status: open, priority: P1,
       finding: "Single-neuron capability LIMIT (XOR / linear-separability) absent from both files (grep: 0)",
       fix_shape: "add 'A single neuron draws one line; XOR needs a hidden layer' with a plotted XOR-scatter or runnable evidence",
       retro_note: "Merges BL-P1a; S3 now requires a capability-limit row in the contract for the next module"}
    # --- v7.6 seed-stabilization: Learning Experience Gate P1s — RESOLVED in this loop ---
    - {id: LX-D1-PICTURE-ORDER, lesson: D1, source: "v7.5 Learning Experience Gate", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "Mental picture (brain analogy, S2) arrived after S1 introduced neuron/weighted-sum/dot-product/bias vocabulary",
       fix_applied: "Seeded a 2-3 sentence brain/'weigh -> add -> decide' picture at top of S1 before any vocabulary; S2 reworded to 'now make that picture exact' as the payoff mapping",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped seed"}
    - {id: LX-D1-OPENING-CHECKLIST, lesson: D1, source: "v7.5 Learning Experience Gate", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "Hero bridged from engineering ('multiply matrices') + Goal box was a capability checklist, not curiosity",
       fix_applied: "Hero now opens with the GPT-3 49,152-columns curiosity hook + a felt one-liner; Goal box demoted to a quiet 'by the time you close this tab' promise",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped seed"}
    - {id: LX-D2-FORMULA-FIRST, lesson: D2, source: "v7.5 Learning Experience Gate", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "S1 'why it exists' led with matrix identity (x@W1)@W2 = x@(W1@W2) before any felt intuition (violated 'no formula before felt intuition')",
       fix_applied: "S1 now opens with plain words + a straight-ruler-on-ruler picture for the collapse; matrix identity deferred to S4 Math Ladder where it already lives (anchor pair [[7,2],[3,1]] unchanged)",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped seed"}
    - {id: LX-D2-NARRATIVE-SPINE, lesson: D2, source: "v7.5 Learning Experience Gate", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "Valve/dimmer analogy reused in build viz but not in the staff-lens dead-ReLU failure or the artifact — thin spine",
       fix_applied: "Carried the valve into the dead-ReLU failure ('one-way valve jammed shut') and the trade-off (Leaky/GELU = 'valve with a permanent small leak'); Produce reframed around the valve/collapse",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped seed"}
    - {id: LX-ARTIFACT-AS-DISCOVERY, lesson: "D1 & D2", source: "v7.5 Learning Experience Gate", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "Produce framed as 'leave today's evidence' + 'Acceptance criteria' (homework register), not 'run it and observe something surprising'",
       fix_applied: "D1 Produce = 'predict the output, then run it and see whether ReLU zeroed it'; D2 Produce = 'watch two layers collapse, then insert ReLU and watch it break'; 'Acceptance criteria' -> 'What you should see'; lesson_audit.py heuristic broadened to accept the discovery register (so it does not false-fire on future modules)",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped seed; run paths, anchor numbers, and explain-back log preserved"}
    - {id: LX-D3toD9-SWEEP, lesson: "D3-D9", source: "v7.6 propagation sweep (v7.5 Learning Experience Gate)", status: fixed, priority: P1,
       resolved_in: "v7.6-seed-stabilization",
       finding: "D3-D9 were built from the same pre-v7.5 template as D1/D2 and carried the same LX inversions: mental picture in Section 2 after Section 1 vocabulary (PICTURE-ORDER, all 7); engineering-bridge/checklist openings (OPENING-CHECKLIST: D3,D4,D6,D7,D8; P2 on D9); a formula stated in Section 1 before intuition (FORMULA-FIRST: D5,D7,D8; P2 on D6); decorative analogy not carried into staff-lens/Produce (NARRATIVE-SPINE: D3,D4,D6,D7,D8,D9); homework-register Produce (ARTIFACT-AS-DISCOVERY, all 7)",
       fix_applied: "Same targeted reorder+reframe as D1/D2, tailored per lesson (assembly-line/golf/foggy-hill/free-throws/rolling-ball/stepping-stones/exam): picture-before-vocabulary seed, curiosity opening, analogy spine into the staff-lens failure, discovery-framed Produce ('What you should see'); D5 deferred the update-rule formula to Section 4, D8 removed the Section 1 formula callout",
       verified: "lesson_audit 9/9 OK (0 advisories); nav 0 BROKEN; staff-lens 9/9 gap 0, q:4 o:16 errs:[]; quest-ids + 7-section + anchors intact per grep",
       retro_note: "would-be P0 on a fresh v7.5 build; P1 for shipped module (correct content, strong payoff)"}

  p2:
    - {id: BL-P2-D1, lesson: D1, finding: "determinism criterion not in Option A/B; 2->3 width in layer SVG (contract-intended); bilingual mid-lesson callouts (user-pref P2)"}
    - {id: BL-P2-D2, lesson: D2, finding: "Option A softens 'cure' (relu insertion) to prose vs Option B numeric diff"}
    - {id: BL-P2-D3, lesson: D3, finding: "playground uses unseeded np.random.rand (prints shapes only; benign)"}
    - {id: BL-P2-D4, lesson: D4, finding: "MSE=0-at-match not printed in playground; bowl-in-ŷ waived (C12); confident-wrong is prose"}
    - {id: BL-P2-D6, lesson: D6, finding: "Produce Option-A acceptance omits 'prints labeled stages' bullet"}
    - {id: BL-P2-D7, lesson: D7, finding: "stub comment 'open lesson.html' vs contract 'open lesson.html, section 7'"}
    - {id: BL-P2-D8, lesson: D8, finding: "playground 'toobig' rounds to 2dp vs 3dp elsewhere"}
    - {id: BL-P2-D9, lesson: D9, finding: "gap take says 'around step 300' while minimum is step 200 (day-09:623); kept-model anchor correctly 200 elsewhere"}
    - {id: BL-P2-review, lesson: review.html, finding: "stale self-check day-tags 'Day 1/2/3' should read 'Day 4/5/6' (L265-267); wf2-review not a hub tile (frozen MODULES array)"}
    - {id: BL-P2-loopvec, lesson: "D2/D3", source: "held-out eval §3", finding: "loop-vs-vectorized + speed benchmark absent; beginner 'common misconceptions' sweep absent (ref has both)"}
    # --- v7.6 seed-stabilization: Learning Experience Gate P2s ---
    - {id: LX-D1-FRONTIER-EARLY, lesson: D1, source: "v7.5 Learning Experience Gate", status: fixed,
       finding: "GPT-3 scale teaser landed in S1 before the learner was oriented by the S2 analogy",
       fix_applied: "Promoted the GPT-3 hook to the hero as the opening curiosity; the S1 picture now precedes the S1 industry callout — frontier relevance follows orientation"}
    - {id: LX-D2-HUMAN-SITUATION, lesson: D2, source: "v7.5 Learning Experience Gate", status: open,
       finding: "No familiar HUMAN situation (valve/dimmer are mechanical); minor — the 'mysterious third step' hook carries the opening. Left as-is to keep the valve spine coherent."}
    - {id: LX-BOTH-GOAL-REGISTER, lesson: "D1 & D2", source: "v7.5 Learning Experience Gate", status: partial,
       finding: "'By the end, you'll be able to:' objective register reads assessment-like. D1 softened in v7.6; D2 goal box kept (house style, hero already strong)."}

# -----------------------------------------------------------------------------
# HELD-OUT EVAL FINDINGS  (v7.2 eval vs 00-neural-networks/fundamentals notebooks)
# -----------------------------------------------------------------------------
heldout_eval_findings:
  yardstick:
    - "00-neural-networks/fundamentals/01_what_is_a_neural_network.ipynb (13 cells; 1 real matplotlib plot)"
    - "00-neural-networks/fundamentals/02_single_neuron.ipynb (51 cells; 16 code cells render real plots)"
    - "00-neural-networks/fundamentals/03_activation_functions.ipynb (41 cells; ~15 executed code cells)"
  headline: >
    m02 is STRONGER than the reference on conceptual depth (brain->math table, scalars-first
    collapse, logits naming, staff-lens, interview framing). But on runnable evidence, plotted
    behavior, capability limits, and claim-grounding, a plain beginner notebook BEATS the polished
    module — the 'over-credited polish' failure mode. Systemic (skills), not module-P0.
  overclaims:
    items:
      - {id: O1, where: "D1 hero/Produce :335,:463", claim: "implies shipped runnable evidence", reality: "experiment.py is 5-line stub", tag: "module-P1", maps_to: BL-P1-legacy}
      - {id: O2, where: "D1 interview :436", claim: "recites 'shifts the decision boundary'", reality: "boundary appears ONLY in that line (grep-confirmed); never drawn", tag: "systemic (S4)", maps_to: BL-VE-D1}
      - {id: O3, where: "D3 §4 + quiz Q4 + interview", claim: "(xW1)W2=x(W1W2) taught 3 places", reality: "shown in 0 in D3 (collapse IS shown in D2)", tag: "systemic (show-the-failure)", maps_to: VE-D2}
      - {id: O4, where: "D2 Produce Option B :460", claim: "'runs it for you'", reality: "/frontier-experiment-lab uninstalled; dead path", tag: "curriculum-wide P1", maps_to: BL-P1-legacy}
    acceptable_not_overclaim: ["universal-approximation hedged 'loosely' (D2:411)", "deferred symmetry-init note (D1 -> D3/D5)"]
  reference_teaches_m02_thin:
    items:
      - {concept: "decision-boundary geometry", ref: "derived+plotted (02: cells 13-17,27-31,36)", m02: "prose only", adjudication: "systemic -> BL-VE-D1"}
      - {concept: "XOR / linear-separability", ref: "plotted (02: cells 40-41)", m02: "absent", adjudication: "Should-cover -> BL-COV-XOR"}
      - {concept: "plotted derivatives", ref: "every curve+derivative (03: cells 12,17,22,27,37)", m02: "prose 'slope≈0.25'", adjudication: "systemic -> BL-VE-D2"}
      - {concept: "Leaky ReLU / softmax", ref: "full formula+code+plot (03)", m02: "named only (D2:419,:425)", adjudication: "names-it != covers-it (aware-tier ok; S2 rule)"}
      - {concept: "loop-vs-vectorized + benchmark", ref: "3 impls+timing (02 cell 10)", m02: "absent", adjudication: "module-P2 -> BL-P2-loopvec"}
    acceptable_scope: ["Step/ELU/PReLU, temperature, He/Xavier guide — beginner day vs full notebook"]
  visual_shortfall: >
    Reference: real executed matplotlib (rendered network, 16-plot geometry suite, every activation
    curve WITH derivative, 2x5 grid printing 'Sigmoid max=0.25 / ReLU grad=1'). m02 D1/D2/D3: zero
    executed plots — simulated fake terminals (literal HTML strings in DEMOS) + static SVG with baked
    numbers. Dead infra: D1 has build-embed iframe CSS but no <iframe> (grep-confirmed). Capability
    proven on D5/D7/D8. -> S1 target.
  artifact_shortfall: >
    Reference: self-contained, ~15 runnable cells/notebook incl. stable-softmax + speed benchmark.
    m02: guided-stub experiment.py + empty log.md. Adjudicated: stub is deliberate (§31), NOT a defect;
    genuine finding = only 'auto-build+run' path is the dead /frontier-experiment-lab -> BL-P1-legacy.
  conclusion: "Systemic skill gaps S1-S4 generalize beyond m02; patch skills (done), not lessons. m02 remains Pass with P1."
  adversarial_self_check: "Did not let staff-lens polish offset evidence gap; did not inflate deliberate design into defect; confirmed m02 numbers correct (evidence-medium gap, not accuracy)."

# -----------------------------------------------------------------------------
# SKILL-GAP CANDIDATES  (systemic; S1-S4 patched in Phase 3)
# -----------------------------------------------------------------------------
skill_gap_candidates:
  patched:
    - id: S1
      skill: frontier-visual-evidence-builder
      gap: "'changing quantity' rule did not separate BEHAVIORAL (curve/derivative/boundary/gradient moving) from STRUCTURAL diagrams; static SVG / prose table accepted as P0 visual on behavioral topics."
      patch: "Added 'Behavioral visual rule' — behavioral concepts require plotted/live-recomputed viz; static SVG/prose table not enough for P0 behavioral (unless explicitly scoped static intuition). Extended show-the-failure + saturation example."
      status: patched
      report: "sessions/m02_v7_2_skills_patch_report.md"
      m02_delta_items: [BL-VE-D1, BL-VE-D2]
    - id: S2
      skill: frontier-refactor-qa
      gap: "Gate credited 'names it' as 'covers it' and prose assertion for behavioral claims; silently waved the dead /frontier-experiment-lab path."
      patch: "Added names-it!=covers-it, prose-for-behavioral, held-out-yardstick step, dead-escape-hatch flag (>=P1). Added P0 trigger 'behavioral P0 concept lacks plotted/live evidence'."
      status: patched
      report: "sessions/m02_v7_2_skills_patch_report.md"
      m02_delta_items: [BL-VE-D1, BL-VE-D2, BL-P1-legacy]
    - id: S3
      skill: frontier-curriculum-architect
      gap: "Coverage contracts enumerated forward formulas but not capability LIMITS (single neuron -> XOR) or geometric interpretation (decision boundary)."
      patch: "Added lens #10 'Capability-Limits & Geometry Lens' — every core mechanism contract adds a capability-limit row + a geometry row paired with a plotted/behavioral visual requirement."
      status: patched
      report: "sessions/m02_v7_2_skills_patch_report.md"
      m02_delta_items: [BL-COV-XOR, BL-VE-D1]
    - id: S4
      skill: frontier-lesson-builder
      gap: "Scripted interview/staff answer could assert a picture/result the lesson never taught (D1:436 'decision boundary')."
      patch: "Added 'Interview-answer grounding rule' — interview claims may only reference things taught/shown earlier; trace each claim to a taught moment; if unanchored, teach it or cut it."
      status: patched
      report: "sessions/m02_v7_2_skills_patch_report.md"
      m02_delta_items: [BL-VE-D1]
    - id: S5
      skill: "frontier-lesson-builder + frontier-refactor-qa (v7.5 Learning Experience Gate) + frontier-curriculum-architect (v7.6 Seed Propagation Risk Rule)"
      gap: "Seed/template lessons built pre-v7.5 place the mental picture in S2 after S1 definitions/formulas, and frame Produce as homework — the v7.5 desired pattern (human situation -> curiosity -> analogy -> tiny example -> mechanism -> artifact-as-discovery) must gate cloned modules so the inversion does not propagate."
      patch: "v7.5 added the Learning Experience Gate (Experience->Mechanism->Staff layering; no formula before felt intuition; artifact-as-discovery). v7.6 added the Seed Propagation Risk Rule + Seed Stabilization Phase so a seed-module LX P1 becomes rollout-blocking before the next module even though it stays P1 for the shipped seed."
      status: patched
      report: "sessions/m02_v7_6_seed_stabilization_report.md"
      m02_delta_items: [LX-D1-PICTURE-ORDER, LX-D1-OPENING-CHECKLIST, LX-D2-FORMULA-FIRST, LX-D2-NARRATIVE-SPINE, LX-ARTIFACT-AS-DISCOVERY]
  not_patched:  # documented, not skill-text gaps
    - "Unfilled experiment.py stub — deliberate learn-by-building (§31)"
    - "Dead /frontier-experiment-lab — infra/uninstall P1, not skill text (S2 now flags it)"
    - "Bilingual callouts — user-preference P2; skills already light-touch"
    - "HTML-lesson vs Jupyter medium — legitimate; m02 hosts live viz on D5/D7/D8"

# -----------------------------------------------------------------------------
# LOOP HISTORY
# -----------------------------------------------------------------------------
loop_history:
  - loop: v7
    outcome: "QA gap report — BLOCKED on 6 P0 (D2 tanh, D5 backprop artifact, D8/D9 anchor-spine, ...)"
    reports: ["sessions/m02_v7_gap_report.md (archived; deleted in working tree)"]
  - loop: v7.1
    outcome: "P0 fix pass — closed 4 blockers (D1 foundation framing, D2 tanh/coverage, D5 backprop artifact, D9 test split); claimed Pass with P1"
    reports: ["sessions/m02_v7_1_gap_report.md", "sessions/m02_v7_1_p0_fix_report.md (archived; deleted in working tree)"]
  - loop: v7.2
    outcome: "Independent re-derived audit: 0 P0, Pass with P1 (0 files edited). Held-out eval found systemic S1-S4. Skills patched (additive, mirror-synced)."
    reports:
      - "sessions/m02_v7_2_qa_report.md"
      - "sessions/m02_v7_2_fix_report.md"
      - "sessions/m02_v7_2_heldout_eval_report.md"
      - "sessions/m02_v7_2_skills_patch_report.md"
  - loop: v7.3-manifest-reconcile
    date: "2026-07-08"
    outcome: >
      Created this manifest as durable source of truth; reconciled all v7.2 findings; applied the
      S1-S4-patched skills as a report-only DELTA QA (grep-verified) against m02. Delta surfaced
      3 new P1 upgrade items (BL-VE-D1, BL-VE-D2, BL-COV-XOR) under retroactive S1/S3/S4 gates —
      all grandfathered P1 (no conflicting model, numbers correct, live-viz capability proven on
      D5/D7/D8). NO new module P0. Status -> pass_with_p1. Module 2 ready as seed module.
    delta_qa_verdict:
      new_p0: 0
      new_p1: ["BL-VE-D1", "BL-VE-D2", "BL-COV-XOR"]
      blocker_report_written: false   # only written on new-P0; none found
      seed_module_ready: true
    verification:
      - "grep: iframes D1/D2/D3=0, D5/D7/D8=1 (gradient-descent.html) — live-viz capability proven"
      - "grep: D1 'boundary' only at :436 (interview line) — S4 ungrounded claim confirmed"
      - "grep: D2 derivative=0, svg=7 static — S1 behavioral gap confirmed"
      - "grep: XOR/perceptron/linear-separable = 0 in D1+D2 — S3 limit gap confirmed"
      - "grep: D7 <table>=0 — P1-b confirmed; quest-ids wf2/wf3 intact — frozen invariants confirmed"
    tooling_from_prior_loops:  # re-usable audit evidence (v7.2)
      - "python3 sessions/lesson_audit.py m02-the-neuron -> 9 OK / 0 MISSING / 0 LEFTOVER / 0 DEGRADED"
      - "python3 sessions/nav_audit.py -> 0 BROKEN"
      - "node sessions/staff_lens_audit.js m02 -> 9/9 staff-lens, gap 0, errs:[], q:4 o:16 (render:BROKEN benign)"
      - "node --check on 11 inline script blocks -> ALL PASS"
      - "jsdom render check NOT run (MODULE_NOT_FOUND) — substituted node syntax + structural checks"
  - loop: v7.6-seed-stabilization
    date: "2026-07-08"
    trigger: "v7.6 Autonomous Rollout Loop — Seed Propagation Risk Rule fired before m03 rollout because sessions/m02_v7_5_learning_experience_audit.md named 5 seed-propagation LX P1s that would replicate into every cloned module."
    scope: "Targeted prose reorder + reframe across ALL 9 lessons: D1-D2 first (seed), then D3-D9 (propagation sweep). Each lesson: hero curiosity opening (checklist demoted), Section 1 picture-seed before vocabulary, Section 2 bridge, analogy spine into the staff-lens failure, and a predict-then-run discovery Produce. No structural/JS/mechanism/staff/artifact-contract changes. Not a full rewrite (no LX P0 found)."
    d3_d9_sweep: >
      D3 (forward pass / assembly-line), D4 (loss / golf score), D5 (backprop / foggy-hill),
      D6 (training loop / basketball free-throws), D7 (optimizers / rolling ball),
      D8 (learning rate / stepping-stones), D9 (train-val-test / exam vs answer-key) each received:
      a picture-before-vocabulary seed at the top of Section 1, a curiosity-led hero with the objective
      list demoted, the analogy carried into the Section 4 staff-lens failure mode, and a predict-then-run
      Produce ("What you should see" replaces "Acceptance criteria"). D5 also deferred the update-rule
      formula out of Section 1; D8 removed the Section 1 formula callout. All anchor numbers, quest-ids
      (wf2-d03 / wf3-d01..d06), BUILD/DEMOS/QS, and completion wiring verified intact.
    outcome: >
      Resolved all 5 LX P1 (LX-D1-PICTURE-ORDER, LX-D1-OPENING-CHECKLIST, LX-D2-FORMULA-FIRST,
      LX-D2-NARRATIVE-SPINE, LX-ARTIFACT-AS-DISCOVERY). Picture now precedes vocabulary on D1;
      hero opens on the GPT-3 curiosity hook; D2 S1 opens with plain words + straight-ruler picture
      with the matrix identity deferred to the S4 Math Ladder; valve analogy carried into the
      dead-ReLU failure + trade-off; both Produce sections reframed to prediction/observation.
      Also broadened sessions/lesson_audit.py Produce heuristic to accept the discovery register.
    verdict:
      new_p0: 0
      resolved_p1: ["LX-D1-PICTURE-ORDER", "LX-D1-OPENING-CHECKLIST", "LX-D2-FORMULA-FIRST", "LX-D2-NARRATIVE-SPINE", "LX-ARTIFACT-AS-DISCOVERY"]
      seed_module_ready: true
      blocker_report_written: false
    verification:
      - "grep: quest-ids wf2-d01-neuron / wf2-d02-activations intact; 7 module-sections each"
      - "grep: D1 anchors x=[2,3] w=[0.5,-1] b=1 ReLU(-1)=0 preserved; D2 anchors [[7,2],[3,1]] [13,4] 0.25 preserved"
      - "python3 sessions/lesson_audit.py m02-the-neuron -> 9 OK / 0 MISSING / 0 LEFTOVER / 0 DEGRADED / 0 advisories"
      - "python3 sessions/nav_audit.py -> 0 BROKEN, PASS"
      - "node sessions/staff_lens_audit.js m02 -> 9/9 staff-lens, gap 0, D1/D2 q:4 o:16 errs:[] (inline JS parses; render:BROKEN benign)"
    reports: ["sessions/m02_v7_6_seed_stabilization_report.md", "sessions/m02_v7_5_learning_experience_audit.md"]
