<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<script>/* set appearance before paint (no flash) */(function(){try{var t=localStorage.getItem('frontier-theme');if(['light','dim','dark','midnight'].indexOf(t)<0)t='dim';document.documentElement.setAttribute('data-theme',t);}catch(e){document.documentElement.setAttribute('data-theme','dim');}})();</script>
<!-- frontier-lang:prepaint -->
<script>/* set reading language before paint (no flash) */(function(){try{var l=localStorage.getItem('frontier-lang');if(['en','zh'].indexOf(l)<0)l='en';document.documentElement.setAttribute('data-lang',l);document.documentElement.setAttribute('lang',l==='zh'?'zh-Hans':'en');}catch(e){document.documentElement.setAttribute('data-lang','en');}})();</script>
<!-- /frontier-lang:prepaint -->
<title>Module 1 · Day 5 — Logs &amp; Exponents</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,600;12..96,700;12..96,800&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
<style>
/* ===========================================================================
   M1 Day 3 — dark "lab notebook" skin, modeled on visual-tutorial/index.html
   (sidebar nav + progress checklist + numbered content column), dark palette.
   Self-contained, offline. Progress persists to localStorage.
   =========================================================================== */
:root{
  /* theme-independent: accents, type, radii, shadows */
  --accent:#8091FF; --accent-d:#6B7BF0; --accent-soft:#1B2340; --on-soft:#EDF1F7;
  --q:#5B9BFF; --k:#B39BFF; --v:#FF9B5C;
  --ok:#5BC98A; --ok-ink:#8FE3AE; --ok-soft:#12271C; --ok-line:#1E7E4C;
  --warn:#E7B04A; --warn-soft:#2A2410; --warn-line:#8A6A18;
  --err:#F07A6A; --err-ink:#F8A99B; --err-soft:#2C1A17; --err-line:#9A3B2C;
  --radius:16px; --r-sm:8px; --r-md:12px; --r-full:9999px;
  --shadow:0 1px 2px rgba(0,0,0,.3), 0 10px 30px rgba(0,0,0,.35);
  --shadow-sm:0 2px 8px rgba(0,0,0,.28);
  --display:'Bricolage Grotesque',Georgia,sans-serif;
  --body:'DM Sans',system-ui,-apple-system,'Segoe UI',sans-serif;
  --mono:'JetBrains Mono','Fira Code',Consolas,monospace;
  --ease:cubic-bezier(.16,1,.3,1);
  /* default surface = Dim (softer charcoal; also the fallback if no data-theme set) */
  --bg:#161C26; --bg2:#0F141C; --panel:#1F2836; --panel2:#273140; --panel3:#2F3A4A;
  --ink:#EDF1F7; --ink2:#BFC9D8; --muted:#93A0B2; --line:#313C4C; --line2:#3E4A5B;
}
/* ---- darkness levels (switchable in the sidebar) ---- */
html[data-theme="dim"]{--bg:#161C26; --bg2:#0F141C; --panel:#1F2836; --panel2:#273140; --panel3:#2F3A4A;
  --ink:#EDF1F7; --ink2:#BFC9D8; --muted:#93A0B2; --line:#313C4C; --line2:#3E4A5B;}
html[data-theme="dark"]{--bg:#10151D; --bg2:#0A0E15; --panel:#18202B; --panel2:#202A36; --panel3:#28323F;
  --ink:#E9EEF5; --ink2:#B8C2D1; --muted:#8B97A8; --line:#28313F; --line2:#35414F;}
html[data-theme="midnight"]{--bg:#090C12; --bg2:#05070B; --panel:#10151D; --panel2:#161D28; --panel3:#1E2732;
  --ink:#E6ECF4; --ink2:#AEB9C8; --muted:#828FA1; --line:#212A37; --line2:#2C3644;}
/* Light mode — the warm cream palette, same notebook layout */
html[data-theme="light"]{
  --bg:#FAF7F2; --bg2:#F1EBE0; --panel:#FFFFFF; --panel2:#FBF7F0; --panel3:#F3EDE3;
  --ink:#2C2A28; --ink2:#55504A; --muted:#6E675F; --line:#E5DFD6; --line2:#D8D0C4;
  --accent:#2A7B9B; --accent-d:#1F6280; --accent-soft:#E4F2F7; --on-soft:#1F6280;
  --q:#1F6280; --k:#5E5191; --v:#9A5A12;
  --ok:#2D8B55; --ok-ink:#1A5C38; --ok-soft:#E8F5EE; --ok-line:#2D8B55;
  --warn:#A67D08; --warn-soft:#FCF3DC; --warn-line:#A67D08;
  --err:#C93B3B; --err-ink:#8F2020; --err-soft:#FDE8E8; --err-line:#C93B3B;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:var(--body);color:var(--ink);background:var(--bg);line-height:1.65;font-size:16.5px;
  -webkit-font-smoothing:antialiased;
  background-image:
    radial-gradient(ellipse 110% 55% at 82% -8%,rgba(128,145,255,.10),transparent 60%),
    radial-gradient(ellipse 90% 50% at 6% 108%,rgba(255,155,92,.06),transparent 60%);
  background-attachment:fixed}
a{color:var(--accent);text-decoration:none}a:hover{text-decoration:underline}
strong{color:var(--ink);font-weight:600}
code{font-family:var(--mono);font-size:.85em;background:var(--bg2);padding:1px 5px;border-radius:5px;color:var(--q);border:1px solid var(--line)}
::-webkit-scrollbar{width:9px;height:9px}::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:var(--line2);border-radius:var(--r-full)}
:focus-visible{outline:2px solid var(--accent);outline-offset:2px;border-radius:4px}

/* ---------- top progress bar ---------- */
#progress-track{position:fixed;top:0;left:0;right:0;height:4px;background:transparent;z-index:60}
#progress-bar{height:100%;width:0%;background:linear-gradient(90deg,var(--q),var(--accent),var(--v));
  transition:width .4s var(--ease);box-shadow:0 0 12px rgba(128,145,255,.5)}

/* ---------- mobile nav toggle ---------- */
#nav-toggle{display:none}

/* ---------- layout ---------- */
.layout{display:flex;align-items:flex-start;max-width:1320px;margin:0 auto}

/* ---------- sidebar ---------- */
#sidebar{position:sticky;top:0;height:100vh;width:288px;flex:0 0 288px;
  display:flex;flex-direction:column;
  padding:22px 0 0;border-right:1px solid var(--line);background:var(--panel)}
.sidebar-scroll{flex:1 1 auto;overflow-y:auto;min-height:0;padding:0 16px}
.brand{padding:6px 8px 14px;border-bottom:1px solid var(--line);margin-bottom:12px}
.brand-title{font-family:var(--display);font-weight:800;font-size:1.2rem;letter-spacing:-.02em}
.brand-dot{color:var(--accent)}
.brand-sub{font-size:.72rem;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin-top:2px}
.nav-count{margin-top:10px;font-family:var(--mono);font-size:.82rem;font-weight:600;color:var(--ink2)}
.nav-count b{color:var(--accent)}
.theme-row{padding:10px 8px 0}
.theme-btns{display:grid;grid-template-columns:1fr 1fr;gap:4px;margin-top:6px;background:var(--panel2);border:1px solid var(--line);border-radius:9px;padding:3px}
.theme-btn{flex:1;font-family:var(--body);font-size:.72rem;font-weight:600;color:var(--muted);background:none;border:0;border-radius:7px;padding:5px 4px;cursor:pointer;transition:.15s}
.theme-btn:hover{color:var(--ink)}
.theme-btn.active{background:var(--accent);color:#fff}
/* frontier-lang:css */
/* ---- reading language (EN / 中文), mirroring .theme-* one-for-one ---- */
.lang-row{padding:10px 8px 0}
.lang-btns{display:grid;grid-template-columns:1fr 1fr;gap:4px;margin-top:6px;background:var(--panel2);border:1px solid var(--line);border-radius:9px;padding:3px}
.lang-btn{font-family:var(--body);font-size:.72rem;font-weight:600;color:var(--muted);background:none;border:0;border-radius:7px;padding:5px 4px;cursor:pointer;transition:.15s}
.lang-btn:hover{color:var(--ink)}
.lang-btn.active{background:var(--accent);color:#fff}
.lang-btn[aria-disabled="true"]{opacity:.38;cursor:not-allowed}
.lang-btn[aria-disabled="true"]:hover{color:var(--muted)}
/* One language shows at a time. A block carrying NEITHER class shows under both,
   which is the fallback: a day with no Chinese twin still reads in English
   instead of going blank. No JavaScript is involved in that decision. */
html[data-lang="en"] .lang-zh{display:none}
html[data-lang="zh"] .lang-en{display:none}
/* CJK needs more leading and no letter-spacing to stay readable at this size.
   No new webfont is loaded — the system CJK stack is used, because every lesson
   page must stay self-contained. */
html[data-lang="zh"] .sec-body,html[data-lang="zh"] .lede,html[data-lang="zh"] .goal{line-height:1.9;letter-spacing:0}
html[data-lang="zh"] .sec-h,html[data-lang="zh"] h1,html[data-lang="zh"] h4{letter-spacing:0}
/* A language wrapper must be typographically transparent. Two rules above it are
   POSITIONAL, so inserting a wrapper silently re-targets them and the page looks
   wrong while every gate stays green:
     .sec-body p:last-child{margin-bottom:0}  — the last paragraph of EVERY span
       would lose its bottom margin, so two consecutive spans' paragraphs touch.
     .sec-body h4:first-child{margin-top:0}   — an h4 opening a LATER span would
       lose its 1.4rem top margin and crowd the prose above it.
   These restore the unwrapped spacing, then re-apply the original intent only
   where the wrapper really is first or last inside .sec-body. */
.sec-body .lang-en>p:last-child,.sec-body .lang-zh>p:last-child{margin-bottom:.9rem}
.sec-body .lang-en>h4:first-child,.sec-body .lang-zh>h4:first-child{margin-top:1.4rem}
.sec-body>.lang-en:first-child>h4:first-child,.sec-body>.lang-zh:first-child>h4:first-child{margin-top:0}
/* /frontier-lang:css */
.nav-group-label{font-size:.68rem;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.09em;padding:4px 8px;margin-top:10px}
.nav-link{display:flex;align-items:center;gap:.5rem;padding:7px 10px;margin:1px 0;border-radius:9px;color:var(--ink2);
  font-size:.86rem;cursor:pointer;border-left:3px solid transparent;transition:all .12s;background:none;border-top:0;border-right:0;border-bottom:0;width:100%;text-align:left;font-family:var(--body)}
.nav-link:hover{background:var(--panel2);text-decoration:none;color:var(--ink)}
.nav-link.active{background:var(--accent-soft);color:var(--on-soft);font-weight:700;border-left-color:var(--accent)}
.nav-link .nl-dot{width:7px;height:7px;border-radius:50%;background:var(--line2);flex:0 0 auto}
.nav-link.done .nl-dot{background:var(--ok)}
.checklist-wrap{margin-top:16px;border-top:1px solid var(--line);padding-top:12px;padding-bottom:16px}
#checklist{list-style:none;margin:0;padding:0}
#checklist li{display:flex;gap:8px;align-items:flex-start;font-size:.78rem;color:var(--ink2);padding:4px 8px;cursor:pointer;border-radius:7px}
#checklist li:hover{background:var(--panel2)}
#checklist li .box{width:15px;height:15px;border-radius:4px;border:1.5px solid var(--line2);flex:0 0 auto;margin-top:2px;
  display:grid;place-items:center;font-size:.66rem;color:var(--bg);font-weight:800}
#checklist li.done .box{background:var(--ok);border-color:var(--ok)}
#checklist li.done .box::after{content:'✓'}
#checklist li.done span{color:var(--muted);text-decoration:line-through}
.ghost-btn{background:none;border:1px solid var(--line2);color:var(--muted);font-size:.74rem;padding:6px 11px;border-radius:8px;cursor:pointer;margin-top:10px;transition:.15s}
.ghost-btn:hover{border-color:var(--err);color:var(--err)}
.side-nav{flex:0 0 auto;padding:12px 16px 16px;border-top:1px solid var(--line);background:var(--panel2);display:flex;flex-direction:column;gap:6px}
.side-nav a{display:flex;flex-direction:column;gap:1px;padding:8px 10px;border:1px solid var(--line2);border-radius:9px;background:var(--panel);color:var(--ink);transition:.15s}
.side-nav a:hover{border-color:var(--accent);text-decoration:none;transform:translateY(-1px)}
.side-nav .d{font-family:var(--mono);font-size:.62rem;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}
.side-nav .t{font-size:.82rem;font-weight:600}

/* ---------- main content ---------- */
#content{flex:1 1 auto;min-width:0;padding:44px clamp(20px,5vw,60px) 80px;max-width:920px}
.hero{border-bottom:1px solid var(--line);padding-bottom:26px;margin-bottom:8px}
.kicker{display:inline-block;font-family:var(--mono);font-size:.72rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;
  color:var(--accent);background:var(--accent-soft);padding:5px 12px;border-radius:999px}
.hero h1{font-family:var(--display);font-size:clamp(2rem,4.6vw,2.9rem);letter-spacing:-.03em;margin:.35em 0 .1em;line-height:1.06}
.hero h1 .sub{display:block;font-size:.5em;font-weight:600;color:var(--ink2);margin-top:.35rem;letter-spacing:-.01em}
.lede{font-size:1.1rem;color:var(--ink2);max-width:64ch;line-height:1.55}
.goal{margin-top:1.3rem;display:flex;gap:.8rem;background:linear-gradient(120deg,var(--accent-soft),transparent);
  border:1px solid var(--accent-d);border-radius:var(--r-md);padding:1rem 1.2rem;max-width:80ch}
.goal .gic{font-size:1.2rem;flex:0 0 auto}.goal b{color:var(--accent)}.goal code{color:var(--ink)}

.module-section{padding:30px 0;border-bottom:1px solid var(--line);scroll-margin-top:20px}
.module-section:last-of-type{border-bottom:none}
.sec-head{display:flex;align-items:center;gap:12px;margin-bottom:12px}
.sec-num{flex:0 0 auto;width:32px;height:32px;border-radius:9px;background:var(--accent-soft);color:var(--accent);
  font-family:var(--mono);font-weight:800;display:grid;place-items:center;font-size:.92rem}
.sec-num.s-what{background:rgba(91,155,255,.16);color:var(--q)}
.sec-num.s-study{background:rgba(179,155,255,.16);color:var(--k)}
.sec-num.s-play{background:rgba(255,155,92,.16);color:var(--v)}
.sec-num.s-code{background:rgba(231,176,74,.16);color:var(--warn)}
.sec-num.s-quiz{background:rgba(231,176,74,.16);color:var(--warn)}
.sec-num.s-produce{background:rgba(91,201,138,.16);color:var(--ok)}
.module-section.done .sec-num{background:var(--ok);color:var(--bg)}
.sec-h{font-family:var(--display);font-size:1.4rem;font-weight:700;letter-spacing:-.02em;line-height:1.2;flex:1;min-width:0}
.sec-tag{font-family:var(--mono);font-size:.66rem;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--muted)}
.sec-body h4{font-family:var(--display);font-size:1.02rem;font-weight:700;color:var(--ink);margin:1.4rem 0 .5rem}
.sec-body h4:first-child{margin-top:0}
.sec-body p{margin-bottom:.9rem;color:var(--ink2)}.sec-body p:last-child{margin-bottom:0}
.sec-body ul,.sec-body ol{margin:0 0 .9rem 1.4rem;color:var(--ink2)}.sec-body li{margin-bottom:.45rem}
.term{border-bottom:1.5px dashed var(--accent);font-weight:500;color:var(--ink);cursor:help}
.term[data-tip]:focus-visible{outline:2px solid var(--accent);outline-offset:2px;border-radius:3px}

/* ---------- cards / callouts ---------- */
.relate{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin:16px 0}
.relate .card{background:var(--panel2);border:1px solid var(--line2);border-radius:var(--radius);padding:16px 18px;box-shadow:var(--shadow-sm)}
.relate .card h5{font-family:var(--display);font-size:.98rem;font-weight:700;color:var(--ink);margin-bottom:.4rem;display:flex;align-items:center;gap:.5rem}
.relate .card .big{font-size:1.5rem}
.relate .card p{font-size:.88rem;color:var(--ink2);margin:0}
.callout{border-radius:var(--radius);padding:15px 18px;margin:16px 0;border:1px solid var(--line2);background:var(--panel2);
  box-shadow:var(--shadow-sm);display:flex;gap:.8rem;font-size:.92rem;line-height:1.55}
.callout .ic{font-size:1.05rem;flex:0 0 auto;line-height:1.4}
.callout div{color:var(--ink2)}.callout b{color:var(--ink)}
.callout code{background:rgba(128,128,128,.2)}
.c-info{background:linear-gradient(180deg,rgba(91,155,255,.10),transparent);border-color:var(--accent-d)}
.c-warn{background:linear-gradient(180deg,rgba(231,176,74,.10),transparent);border-color:var(--warn-line)}
.c-ok{background:linear-gradient(180deg,rgba(91,201,138,.10),transparent);border-color:var(--ok-line)}
.takeaway{background:var(--panel2);border-left:3px solid var(--accent);padding:11px 15px;border-radius:0 10px 10px 0;font-size:.95rem;margin:14px 0;color:var(--ink2)}

/* ---------- got-it button ---------- */
.gotit{margin-top:1.3rem;display:inline-flex;align-items:center;gap:.5rem;font-family:var(--body);font-size:.92rem;font-weight:700;
  color:#fff;background:var(--accent);border:1.5px solid var(--accent);border-radius:10px;padding:.62rem 1.25rem;cursor:pointer;
  transition:background .18s,transform .1s,box-shadow .18s}
.gotit:hover{background:var(--accent-d);box-shadow:0 6px 20px rgba(128,145,255,.28)}
.gotit:active{transform:translateY(1px)}
.gotit:disabled{opacity:.45;filter:grayscale(.5);cursor:not-allowed;box-shadow:none}
.gotit:disabled:hover{background:var(--accent);transform:none}
.module-section.done .gotit{background:var(--ok);border-color:var(--ok)}

/* ---------- playground (fake terminal) ---------- */
.viz-panel{background:var(--panel);border:1px solid var(--line2);border-radius:var(--radius);padding:18px;margin:18px 0;box-shadow:var(--shadow-sm)}
.play-btns{display:flex;flex-wrap:wrap;gap:.5rem;margin-bottom:.8rem}
.play-btn{font-family:var(--mono);font-size:.8rem;font-weight:500;border:1px solid var(--line2);background:var(--panel2);color:var(--ink2);
  border-radius:var(--r-sm);padding:.5rem .8rem;cursor:pointer;transition:all .15s}
.play-btn:hover{border-color:var(--v);color:var(--ink)}
.play-btn.active{background:var(--v);color:var(--bg2);border-color:var(--v);font-weight:600}
.play-btn.seen:not(.active){border-color:var(--ok);color:var(--ok-ink)}
.play-btn.seen::after{content:' ✓';font-weight:700}
.play-console{background:#080B11;color:#CDD6F4;font-family:var(--mono);font-size:.84rem;line-height:1.7;padding:1rem 1.15rem;
  border-radius:var(--r-md);border:1px solid var(--line);min-height:160px;white-space:pre-wrap;overflow-x:auto}
.play-console .prompt{color:#5C6675}.play-console .kw{color:#C9A9F5}.play-console .str{color:#8FE3A0}
.play-console .num{color:#F0B366}.play-console .fn{color:#7BB5F5}.play-console .ok{color:#8FE3A0}
.play-console .bad{color:#F08B8B}.play-console .dim{color:#8891A0}.play-console .hl{color:#F5DC8A}
.play-take{margin-top:.75rem;padding:.8rem .95rem;font-size:.85rem;background:var(--panel2);border:1px solid var(--line);
  border-radius:var(--r-md);color:var(--ink2);line-height:1.5;min-height:2.4em}
.play-take b{color:var(--ink)}
.hint{font-size:.85rem;color:var(--muted);margin-top:.8rem}

/* ---------- build (scroll reveal) ---------- */
.build{display:flex;flex-direction:column;gap:1rem;margin:1rem 0}
.build-hint{font-family:var(--mono);font-size:.74rem;letter-spacing:.06em;color:var(--muted);text-align:center;margin:.2rem 0 .5rem}
.build-step{display:grid;grid-template-columns:1fr;gap:.7rem;padding:1.05rem 1.15rem;border:1px solid var(--line2);border-radius:var(--r-md);
  background:var(--panel2);opacity:1;transform:none;transition:opacity .55s var(--ease),transform .55s var(--ease)}
.build.armed .build-step:not(.revealed){opacity:0;transform:translateY(18px)}
.build-viz{display:block;text-align:center;min-height:56px;overflow-x:auto;padding:.7rem;background:#F7F9FB;border-radius:var(--r-sm)}
.build-viz svg{display:inline-block;width:100%;max-width:500px;height:auto;vertical-align:middle}
.build-note{font-size:.9rem;color:var(--ink2);line-height:1.55}.build-note b{color:var(--ink)}.build-note code{background:var(--bg2)}
.build-num{display:inline-flex;align-items:center;justify-content:center;width:22px;height:22px;border-radius:50%;background:var(--accent);color:#fff;
  font-family:var(--mono);font-size:.72rem;font-weight:700;margin-right:.5rem;vertical-align:middle}
.build-embed{border:1px solid var(--line2);border-radius:var(--r-md);overflow:hidden;background:#F7F9FB;margin:1rem 0}
.build-embed iframe{width:100%;height:520px;border:0;display:block;background:#F7F9FB}
.build-embed .cap{display:flex;align-items:center;gap:.6rem;font-family:var(--mono);font-size:.72rem;color:var(--muted);padding:.5rem .8rem;background:var(--panel2);border-top:1px solid var(--line)}
.build-embed .cap a{color:var(--accent)}
.build-embed .cap .reload{margin-left:auto;font-family:var(--body);font-size:.7rem;font-weight:600;color:var(--ink2);background:var(--panel3);border:1px solid var(--line2);border-radius:var(--r-sm);padding:.3rem .6rem;cursor:pointer}
.build-embed .cap .reload:hover{border-color:var(--accent);color:var(--accent)}

/* ---------- quiz ---------- */
.q{margin-bottom:1.6rem}.q:last-child{margin-bottom:0}
.q-ask{font-family:var(--body);font-size:.96rem;font-weight:600;color:var(--ink);margin-bottom:.7rem;line-height:1.4}
.q-opts{display:flex;flex-direction:column;gap:.5rem}
.q-opt{display:flex;align-items:center;gap:.7rem;text-align:left;font-family:var(--body);font-size:.9rem;color:var(--ink2);
  border:1.5px solid var(--line2);background:var(--panel2);border-radius:var(--r-sm);padding:.65rem .9rem;cursor:pointer;transition:all .15s}
.q-opt:hover:not(.locked){border-color:var(--accent);color:var(--ink)}
.q-opt .mark{width:20px;height:20px;border-radius:50%;border:2px solid var(--line2);flex:0 0 auto;display:grid;place-items:center;font-size:.7rem;font-weight:800;color:var(--bg)}
.q-opt.correct{border-color:var(--ok);background:var(--ok-soft);color:var(--ink)}
.q-opt.correct .mark{background:var(--ok);border-color:var(--ok)}.q-opt.correct .mark::after{content:'✓'}
.q-opt.wrong{border-color:var(--err);background:var(--err-soft);color:var(--ink)}
.q-opt.wrong .mark{background:var(--err);border-color:var(--err)}.q-opt.wrong .mark::after{content:'✕'}
.q-opt.locked{cursor:default}
.q-fb{max-height:0;overflow:hidden;opacity:0;transition:max-height .3s,opacity .3s;font-size:.86rem;border-radius:var(--r-sm);line-height:1.5}
.q-fb.show{max-height:360px;opacity:1;padding:.75rem .95rem;margin-top:.5rem}
.q-fb.good{background:var(--ok-soft);color:var(--ok-ink);border:1px solid var(--ok-line)}
.q-fb.bad{background:var(--err-soft);color:var(--err-ink);border:1px solid var(--err-line)}
.q-fb code{background:rgba(128,128,128,.2)}

/* ---------- produce prompt ---------- */
.prompt{border:1px solid var(--line2);border-radius:var(--r-md);overflow:hidden;margin:1rem 0}
.prompt-h{display:flex;align-items:center;justify-content:space-between;gap:.7rem;padding:.55rem .6rem .55rem 1rem;background:var(--panel2);border-bottom:1px solid var(--line)}
.prompt-l{font-family:var(--mono);font-size:.74rem;color:var(--ink2)}.prompt-l b{color:var(--accent)}
.copy{font-family:var(--body);font-size:.74rem;font-weight:600;color:#fff;background:var(--accent);border:none;border-radius:var(--r-sm);padding:.45rem .8rem;cursor:pointer;transition:.15s}
.copy:hover{background:var(--accent-d)}.copy.done{background:var(--ok)}
.prompt-t{margin:0;padding:1rem;background:#080B11;color:#C6D2E0;font-family:var(--mono);font-size:.8rem;line-height:1.6;white-space:pre-wrap;word-break:break-word}

/* ---------- completion banner ---------- */
.fin{margin:1.8rem 0 0;padding:2rem;text-align:center;background:linear-gradient(135deg,var(--ok-soft),var(--accent-soft));
  border:1px solid var(--ok);border-radius:var(--radius);opacity:0;max-height:0;overflow:hidden;transition:opacity .4s,max-height .4s}
.fin.show{opacity:1;max-height:480px;padding:2.2rem 2rem}
.fin .em{font-size:2.6rem;display:block;margin-bottom:.5rem}
.fin h3{font-family:var(--display);font-size:1.5rem;font-weight:700;color:var(--ok-ink);margin-bottom:.5rem}
.fin p{color:var(--ink2);font-size:.92rem;margin:0}.fin b{color:var(--ink)}.fin code{background:rgba(0,0,0,.35)}

.site-footer{margin-top:36px;padding-top:20px;border-top:1px solid var(--line);font-size:.8rem;color:var(--muted);line-height:1.6}

/* ---------- tooltip ---------- */
#term-tip{position:fixed;z-index:1000;max-width:min(300px,92vw);background:#080B11;color:#EDEAFE;font-family:var(--body);font-size:.82rem;
  font-weight:400;line-height:1.45;text-align:left;padding:.6rem .78rem;border-radius:var(--r-md);border:1px solid var(--line2);
  box-shadow:var(--shadow);pointer-events:none;opacity:0;transform:translateY(3px);transition:opacity .15s var(--ease),transform .15s var(--ease)}
#term-tip.show{opacity:1;transform:translateY(0)}
#term-tip .ttl{display:block;font-family:var(--mono);font-size:.66rem;letter-spacing:.05em;text-transform:uppercase;color:var(--accent);margin-bottom:.2rem}

/* ---------- responsive ---------- */
#scrim{display:none}
@media(max-width:900px){
  .layout{display:block}
  #sidebar{position:fixed;z-index:70;top:0;left:0;transform:translateX(-100%);transition:transform .22s var(--ease);
    box-shadow:var(--shadow);width:290px;height:100vh}
  #sidebar.open{transform:translateX(0)}
  #nav-toggle{display:inline-flex;align-items:center;gap:.4rem;position:fixed;top:12px;right:12px;z-index:65;
    background:var(--accent);color:#fff;border:none;padding:9px 15px;border-radius:10px;font-weight:700;font-family:var(--body);font-size:.85rem;cursor:pointer;box-shadow:var(--shadow)}
  #scrim.show{display:block;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:69}
  #content{padding:60px 18px 70px;max-width:100%}
  .relate{grid-template-columns:1fr}
}
@media(prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important;scroll-behavior:auto!important}}
/* ── inline SVG visual lab (playground) — dependency-free, theme-aware ── */
.vlab{border:1px solid var(--line2);border-radius:var(--r-md);background:var(--panel2);padding:14px 16px;margin:0 0 16px;box-shadow:var(--shadow-sm)}
.vlab-head{display:flex;flex-wrap:wrap;align-items:baseline;justify-content:space-between;gap:.5rem;margin-bottom:.4rem}
.vlab-title{font-family:var(--display);font-weight:700;font-size:1rem;color:var(--ink)}
.vlab-readout{font-family:var(--mono);font-size:.8rem;color:var(--ink2)}.vlab-readout b{color:var(--accent)}
.vlab-q{font-size:.82rem;font-weight:600;color:var(--ink);background:rgba(128,145,255,.10);border:1px solid var(--accent-d);border-radius:8px;padding:.5rem .7rem;margin:0 0 .7rem;line-height:1.45}
.vlab-panels{display:grid;grid-template-columns:1fr 1fr;gap:12px}
@media(max-width:640px){.vlab-panels{grid-template-columns:1fr}}
.vlab-fig{margin:0}
.vlab-fig figcaption{font-size:.74rem;color:var(--muted);margin-bottom:4px;text-align:center}
.vlab-svg{width:100%;height:auto;display:block;background:#F8FAFC;border:1px solid var(--line);border-radius:8px}
.vlab-axis{stroke:#C2C9D4;stroke-width:1.2}
.vlab-grid{stroke:#E7EAF0;stroke-width:1}
.vlab-lbl{fill:#5A6472;font-family:var(--mono);font-size:11px}
.vlab-lbl.mut{fill:#96A0AF;font-size:10px}
.vlab-fit{stroke:#111827;stroke-width:2;fill:none}
.vlab-fit.dash{stroke:#B0453A;stroke-dasharray:5 4}
.vlab-pt{stroke:#1F2937;stroke-width:1.1}
.vlab-controls{display:flex;flex-wrap:wrap;align-items:center;gap:14px;margin-top:12px}
.vlab-controls label{display:flex;align-items:center;gap:.5rem;font-size:.84rem;color:var(--ink2)}
.vlab-controls input[type=range]{accent-color:var(--accent);cursor:pointer;vertical-align:middle}
.vlab-val{font-family:var(--mono);font-size:.78rem;color:var(--accent);font-weight:600}
.vlab-toggle{display:inline-flex;border:1px solid var(--line2);border-radius:8px;overflow:hidden}
.vlab-btn{font-family:var(--body);font-size:.78rem;font-weight:600;color:var(--ink2);background:var(--panel);border:0;padding:.4rem .7rem;cursor:pointer}
.vlab-btn.active{background:var(--accent);color:#fff}
.vlab-note{font-size:.78rem;color:var(--muted);margin:.6rem 0 0;line-height:1.5}
</style>
</head>

<body data-quest-id="wf1-d05-logs">

<div id="progress-track" aria-hidden="true"><div id="progress-bar" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" aria-label="lesson progress"></div></div>
<button id="nav-toggle" type="button" aria-label="Toggle sections" aria-expanded="false"><span class="lang-en">☰ Sections</span><span class="lang-zh">☰ 目录</span></button>
<div id="scrim" aria-hidden="true"></div>

<div class="layout">

  <!-- ============ SIDEBAR ============ -->
  <aside id="sidebar" aria-label="lesson navigation">
    <div class="sidebar-scroll">
    <div class="brand">
      <div class="brand-title">Frontier<span class="brand-dot">.</span>Lab</div>
      <div class="brand-sub">Foundations · M1 Day 5</div>
      <div class="nav-count" id="count"><b>0</b><span class="lang-en">/7 sections done</span><span class="lang-zh">/7 节完成</span></div>
    </div>

    <div class="theme-row" role="group" aria-label="appearance mode">
      <span class="nav-group-label" style="padding:0"><span class="lang-en">Appearance</span><span class="lang-zh">外观</span></span>
      <div class="theme-btns">
        <button class="theme-btn" data-theme-set="light" type="button">Light</button>
        <button class="theme-btn" data-theme-set="dim" type="button">Dim</button>
        <button class="theme-btn" data-theme-set="dark" type="button">Dark</button>
        <button class="theme-btn" data-theme-set="midnight" type="button">Midnight</button>
      </div>
    </div>

    <!-- frontier-lang:markup -->
    <div class="lang-row" role="group" aria-label="reading language">
      <span class="nav-group-label" style="padding:0">Language · 语言</span>
      <div class="lang-btns">
        <button class="lang-btn" data-lang-set="en" type="button">English</button>
        <button class="lang-btn" data-lang-set="zh" type="button">中文</button>
      </div>
    </div>
    <!-- /frontier-lang:markup -->

    <nav aria-label="Sections">
      <div class="nav-group-label">Module 01 · Represent</div>
      <button class="nav-link" data-target="home"><span class="nl-dot"></span>Start here</button>
      <button class="nav-link" data-target="s1"><span class="nl-dot"></span>1 · What is it</button>
      <button class="nav-link" data-target="s2"><span class="nl-dot"></span>2 · Intuition</button>
      <button class="nav-link" data-target="s3"><span class="nl-dot"></span>3 · <span class="lang-en">Playground</span><span class="lang-zh">试一试</span></button>
      <button class="nav-link" data-target="s4"><span class="nl-dot"></span>4 · Mechanism &amp; why</button>
      <button class="nav-link" data-target="s5"><span class="nl-dot"></span>5 · <span class="lang-en">Build it up</span><span class="lang-zh">一步步搭起来</span></button>
      <button class="nav-link" data-target="s6"><span class="nl-dot"></span>6 · <span class="lang-en">Quiz</span><span class="lang-zh">小测</span></button>
      <button class="nav-link" data-target="s7"><span class="nl-dot"></span>7 · Produce</button>
    </nav>

    <div class="checklist-wrap">
      <div class="nav-group-label"><span class="lang-en">Progress checklist</span><span class="lang-zh">进度清单</span></div>
      <ul id="checklist"><!-- built by JS --></ul>
      <button id="reset" class="ghost-btn" type="button"><span class="lang-en">↺ Reset progress</span><span class="lang-zh">↺ 清空进度</span></button>
    </div>
    </div><!-- /.sidebar-scroll -->

    <div class="side-nav">
      <a class="lnav prev" href="../day-04-matmul-and-shapes/lesson.html"><span class="d"><span class="lang-en">← Prev</span><span class="lang-zh">← 上一天</span></span><span class="t">Matmul &amp; Shapes</span></a>
      <a class="lnav next" href="../day-06-random-seeds/lesson.html"><span class="d"><span class="lang-en">Next →</span><span class="lang-zh">下一天 →</span></span><span class="t">Random Seeds &amp; PRNG</span></a>
      <a class="lnav-hub" href="../../index.html"><span class="d"><span class="lang-en">▦ Map</span><span class="lang-zh">▦ 地图</span></span><span class="t"><span class="lang-en">Back to curriculum</span><span class="lang-zh">回到课程地图</span></span></a>
    </div>
  </aside>

  <!-- ============ MAIN CONTENT ============ -->
  <main id="content">

    <section id="home" class="hero">
      <span class="kicker">Module 1 · Represent · Day 5</span>
      <h1>Logs &amp; Exponents<span class="sub">How to Read a Scaling-Law Plot</span></h1>
      <p class="lede">The last bit of math before real ML. The famous <strong>scaling laws</strong> — the curves that predict how much better a model gets as you spend more compute — are <strong>power laws</strong>. On a log-log plot, a power law becomes a straight line. Learn to read that line and you can read the most important charts in the field.</p>
      <div class="goal"><span class="gic" aria-hidden="true">🎯</span><div><b>By the end, you'll be able to:</b> say what a log is, turn a power law into a straight line, and read the slope of a log-log line as its exponent.</div></div>
    </section>

    <!-- ===== 1. WHAT IS IT ===== -->
    <section class="module-section" id="s1" data-sec="what">
  <div class="sec-head"><span class="sec-num s-what">1</span><span class="sec-h">Exponents, logs, and power laws</span><span class="sec-tag">What is it</span></div>
  <div class="sec-body">
      <div class="callout c-info"><span class="ic">🧭</span><div><b>What this assumes:</b> just arithmetic and Days 1–4. <b>No calculus.</b> This lesson is lighter on code — it's the reading skill for the plots you'll meet in the scaling-law modules.</div></div>

      <h4>What is an exponent?</h4>
      <p>An <span class="term" data-tip="Repeated multiplication: x^b means multiply x by itself b times. 2^3 = 2·2·2 = 8.">exponent</span> is repeated multiplication: <code>2³ = 2·2·2 = 8</code>. The small number on top says how many times.</p>

      <h4>What is a log?</h4>
      <p>A <span class="term" data-tip="The inverse of an exponent. log10(1000)=3 because 10^3=1000. It answers: 10 to what power gives this number?">log</span> asks the reverse question: "10 to <em>what power</em> gives this number?" Since <code>10³ = 1000</code>, <code>log₁₀(1000) = 3</code>. A log tells you the number of <span class="term" data-tip="A factor of 10. Going from 100 to 1000 is one order of magnitude.">orders of magnitude</span>. Log and exponent <strong>undo each other</strong>.</p>

      <h4>What is a power law?</h4>
      <p>A <span class="term" data-tip="A relationship y = a·x^b, where one quantity is a fixed power of another. Scaling laws are power laws.">power law</span> is a relationship <code>y = a·x^b</code> — one thing is a power of another. Scaling laws (loss vs compute) are power laws. The magic you'll use: draw a power law on <strong>log-log</strong> axes and it becomes a straight <strong>line</strong>, whose slope is the exponent <code>b</code>. Keep going 👇</p>
      <div class="callout c-warn"><span class="ic">😕</span><div><b>为什么这里容易卡住 · Why this trips people up:</b> 很多人把 log 的规则背下来，却说不出它到底在问什么，于是一看到 log-log 图就懵了。In plain terms: people memorize the log <em>rules</em> but never anchor the one question a log asks — "10 to what power gives this number?" — so a log-log plot feels like a foreign language instead of the simplest chart in ML.</div></div>
      <div class="callout c-info"><span class="ic">🏭</span><div><b>A real one:</b> DeepMind's Chinchilla paper trained a 70-billion-parameter model on 1.4 trillion tokens — a ratio of 20 tokens per parameter they found by reading the slope of a log-log line, exactly like you're about to.</div></div>
      <button class="gotit" type="button">Got the three words — let's go</button>
    </div>
</section>

    <!-- ===== 2. INTUITION ===== -->
    <section class="module-section" id="s2" data-sec="intuition">
  <div class="sec-head"><span class="sec-num s-study">2</span><span class="sec-h">The Richter scale</span><span class="sec-tag">Intuition</span></div>
  <div class="sec-body">
      <div class="relate">
        <div class="card"><span class="big">📈</span><h5>A log axis = "×10 per step"</h5><p>On a normal ruler, steps add (+1, +2). On a <strong>log</strong> ruler, each step <strong>multiplies</strong> by 10: 1, 10, 100, 1000 are evenly spaced. Earthquakes (Richter) and loudness (decibels) work this way.</p></div>
        <div class="card"><span class="big">📏</span><h5>Why it helps</h5><p>A log turns "times 10" into "plus 1." Huge ranges (1 to a billion) fit on one axis, and any multiply-relationship becomes a straight line you can measure.</p></div>
      </div>
      <p><strong>In one line:</strong> a log squashes a "multiply" world into an "add" world — that's why power laws straighten out on log axes.</p>
      <p><b>直觉 / Intuition:</b> log 就像一把「每格 ×10」的尺子，把庞大的数字压成好读的小数字。The technical point is that a log converts multiplication into addition, so a range from 1 to a billion fits on one axis and any "multiply" relationship becomes a straight line you can measure.</p>
      <p><b>In code:</b> you meet this as <em>log-space</em> — multiplying hundreds of tiny probabilities underflows to <code>0</code>, so libraries add <code>log</code>-probabilities instead. Same "multiply → add" trick, just used to keep the arithmetic safe.</p>
      <div class="callout c-warn"><span class="ic">⚠️</span><div>Where it breaks: logs only work for <strong>positive</strong> numbers. And a power law only becomes a straight line when <strong>both</strong> axes are log (a "log-log" plot) — not just one.</div></div>
      <button class="gotit" type="button">Got the picture</button>
    </div>
</section>

    <!-- ===== 3. PLAYGROUND ===== -->
    <section class="module-section" id="s3" data-sec="play">
  <div class="sec-head"><span class="sec-num s-play">3</span><span class="sec-h"><span class="lang-en">Click through three cases and see for yourself</span><span class="lang-zh">三种情况都点一遍，自己看一看</span></span><span class="sec-tag"><span class="lang-en">Playground</span><span class="lang-zh">试一试</span></span></div>
  <div class="sec-body">
      <p><span class="lang-en">First, <strong>drag the exponent</strong> below and watch the same power law bend on normal axes but snap to a straight line on log-log axes — the slope you read off <em>is</em> the exponent. Then click through the three exact-number cases in the terminal.</span><span class="lang-zh">先玩下面这个：<strong>拖动 exponent</strong>。同一条 power law 在普通轴上是弯的。换到 log-log 轴上，它一下就变直了。你从这条直线上读到的 slope，<em>就是</em>那个 exponent。然后去点终端里那三个带具体数字的例子。</span> <strong><span class="lang-en">Click all three</span><span class="lang-zh">三个都点一遍</span></strong> <span class="lang-en">to finish this section.</span><span class="lang-zh">就算完成这一节。</span></p>
      <div class="vlab">
        <div class="vlab-head">
          <span class="vlab-title"><span class="lang-en">Drag the exponent — watch the curve straighten</span><span class="lang-zh">拖动 exponent —— 看那条曲线变直</span></span>
          <span class="vlab-readout"><span class="lang-en">slope of the log-log line</span><span class="lang-zh">log-log 直线的 slope</span> <b>= b = <span id="lg-b">2.00</span></b></span>
        </div>
        <p class="vlab-q"><span class="lang-en">Learning question: why does a power law <code>y = a·x^b</code> only become a straight line <em>after</em> you take logs — and what sets that line's slope?</span><span class="lang-zh">想一想：为什么一条 power law <code>y = a·x^b</code> 一定要<em>先</em>取 log，才会变成一条直线？又是什么决定了这条线的 slope？</span></p>
        <div class="vlab-panels">
          <figure class="vlab-fig"><figcaption><span class="lang-en">Left · normal axes — a power law bends</span><span class="lang-zh">左边 · 普通轴 —— power law 是弯的</span> <span class="vlab-val" id="lg-left-yr">y max ≈ 1e3</span></figcaption>
            <svg class="vlab-svg" id="lg-left" viewBox="0 0 460 300" role="img" aria-label="On normal axes, y equals x to the power b is a curve that bends."></svg></figure>
          <figure class="vlab-fig"><figcaption id="lg-right-cap">Right · log-log axes — a straight line, slope = b</figcaption>
            <svg class="vlab-svg" id="lg-right" viewBox="0 0 460 300" role="img" aria-label="On log-log axes the same data is a straight line whose slope equals the exponent b."></svg></figure>
        </div>
        <div class="vlab-controls">
          <label><span class="lang-en">exponent b</span><span class="lang-zh">exponent b（指数）</span>
            <input type="range" id="lg-exp" min="-1" max="3" step="0.05" value="2" aria-label="power-law exponent b">
          </label>
          <span class="vlab-val" id="lg-exp-lbl">b = 2.00</span>
          <span class="vlab-toggle" role="group" aria-label="Right-panel scale">
            <button type="button" class="vlab-btn active" id="lg-both" aria-pressed="true"><span class="lang-en">log-log (both axes)</span><span class="lang-zh">log-log（两根轴都取 log）</span></button>
            <button type="button" class="vlab-btn" id="lg-semi" aria-pressed="false"><span class="lang-en">log y only</span><span class="lang-zh">只给 y 取 log</span></button>
          </span>
          <span class="vlab-toggle" role="group" aria-label="Presets">
            <button type="button" class="vlab-btn" id="lg-p2">y = x² (b=2)</button>
            <button type="button" class="vlab-btn" id="lg-pk"><span class="lang-en">Kaplan loss (b=−0.05)</span><span class="lang-zh">Kaplan 的 loss（b=−0.05）</span></button>
          </span>
        </div>
        <div class="callout c-info"><span class="ic">👀</span><div><span class="lang-en"><b>What you should notice:</b> the same data is a bending <b>curve</b> on the left (normal axes) but a perfectly straight <b>line</b> on the right (log-log). Drag <code>b</code> and the log-log line <b>rotates</b> — its slope always equals the exponent you set. Try the <b>Kaplan loss</b> preset: a real scaling exponent is a small <em>negative</em> number, so the line is nearly flat and tilts down.</span><span class="lang-zh"><b>要注意看这个：</b>同样的数据，在左边（普通轴）是一条弯的<b>曲线</b>。在右边（log-log）它是一条笔直的<b>线</b>。拖动 <code>b</code>，log-log 那条线会<b>转</b>起来。它的 slope 永远等于你设的那个 exponent。试试 <b>Kaplan 的 loss</b> 这个按钮：真实的 scaling exponent 是一个很小的<em>负</em>数。所以那条线几乎是平的，还微微往下斜。</span></div></div>
        <div class="callout c-warn"><span class="ic">⚠️</span><div><span class="lang-en"><b>Common misconception:</b> logging <em>only one</em> axis is not enough. Press <b>log y only</b> and the same points stay <b>curved</b> — a power law is a straight line <b>only</b> on log-log (both axes). And the slope you read off <em>is</em> the exponent <code>b</code>, not something you have to guess.</span><span class="lang-zh"><b>一个常见的误会：</b><em>只给一根</em>轴取 log 是不够的。按一下 <b>只给 y 取 log</b>，同样那几个点还是<b>弯</b>的。<b>只有</b>两根轴都取 log，power law 才是一条直线。而且你读到的那个 slope <em>就是</em> exponent <code>b</code>。它不用你去猜。</span></div></div>
        <p class="vlab-note"><span class="lang-en"><b>How to read it:</b> drag <b>exponent b</b> to reshape both panels; use <b>log y only</b> to see why both axes must be log; the presets jump to <code>y=x²</code> and a real scaling-law slope. <b>Where this simplifies reality:</b> the constant <code>a</code> is fixed at 1 — it only slides the line up or down, it does <em>not</em> change the slope — and real scaling-law data is noisy, so points scatter <em>around</em> the line instead of sitting exactly on it.</span><span class="lang-zh"><b>怎么看这两张图：</b>拖 <b>exponent b</b>，左右两张图会一起变。按 <b>只给 y 取 log</b>，你就明白为什么两根轴都得取 log。两个预设按钮会直接跳到 <code>y=x²</code> 和一个真实的 scaling-law slope。<b>这里把现实简化了：</b>常数 <code>a</code> 一直是 1。它只会把整条线往上或者往下挪，<em>不会</em>改变 slope。真实的 scaling-law 数据是有噪声的。所以那些点散<em>在</em>这条线的两边，不会正好落在它上面。</span></p>
      </div>
      <div class="play">
        <div class="play-btns">
          <button class="play-btn" data-demo="explog" type="button"><span class="lang-en">① exp &amp; log undo each other</span><span class="lang-zh">① exp 和 log 互相消掉</span></button>
          <button class="play-btn" data-demo="oom" type="button"><span class="lang-en">② log10 = orders of magnitude</span><span class="lang-zh">② log10 = 数量级</span></button>
          <button class="play-btn" data-demo="power" type="button"><span class="lang-en">③ power law → straight line</span><span class="lang-zh">③ power law → 一条直线</span></button>
        </div>
        <div class="play-console" id="console"><span class="dim"><span class="lang-en"># click a button above to start</span><span class="lang-zh"># 点上面的按钮开始</span></span></div>
        <div class="play-take" id="take"><span class="dim"><span class="lang-en">Each click shows a one-line "what this means" here.</span><span class="lang-zh">每点一次，这里会出现一行「这是什么意思」。</span></span></div>
      </div>
      <p style="font-size:.85rem;color:var(--muted)"><span class="lang-en">Tip: <code>np.log</code> is the natural log (base e); <code>np.log10</code> is base 10. For "orders of magnitude," use <code>log10</code>.</span><span class="lang-zh">小提示：<code>np.log</code> 是自然对数，底数是 e。<code>np.log10</code> 的底数是 10。想数「有几个 orders of magnitude」，就用 <code>log10</code>。</span></p>
      <button class="gotit" type="button" disabled><span class="lang-en">try all three first</span><span class="lang-zh">先把三个都点一遍</span></button>
    </div>
</section>

    <!-- ===== 4. MECHANISM & WHY ===== -->
    <section class="module-section" id="s4" data-sec="why">
  <div class="sec-head"><span class="sec-num s-study">4</span><span class="sec-h">Why a power law is a straight line — and why that runs the industry</span><span class="sec-tag">Mechanism &amp; why</span></div>
  <div class="sec-body">
      <h4>The one derivation</h4>
      <p>Start with a power law and take the log of both sides:</p>
      <div class="callout c-ok"><span class="ic">🧮</span><div><code>y = a · x^b</code>&nbsp;&nbsp;→&nbsp;&nbsp;<code>log y = log a + b · log x</code><br><span style="font-size:.85rem;color:var(--text2)">Rename <code>Y = log y</code> and <code>X = log x</code>: that's <code>Y = (log a) + b·X</code> — the equation of a <strong>straight line</strong> with slope <code>b</code>.</span></div></div>
      <p>So on log-log axes, a power law is a line, and its <strong>slope is the exponent</strong>. Two log rules make it work: <code>log(a·b) = log a + log b</code>, and <code>log(x^b) = b·log x</code>.</p>
      <div class="callout c-info"><span class="ic">🪜</span><div><b>Math Ladder — a power law becomes a straight line</b>
      <br><b>1 · In words:</b> take the log of both sides of a power law and the curve turns into a straight line whose steepness is the exponent.
      <br><b>2 · The formula:</b> <code>y = a·x^b</code> → <code>log y = log a + b·log x</code> — <code>y</code>, <code>x</code> = the two quantities; <code>a</code> = a constant that sets the height; <code>b</code> = the exponent you want. Rename <code>Y = log y</code>, <code>X = log x</code> and it reads <code>Y = (log a) + b·X</code>, a line with slope <code>b</code>.
      <br><b>3 · Tiny numbers:</b> <code>y = x²</code> with <code>x = 1, 10, 100</code> → <code>y = 1, 100, 10000</code>. Take <code>log₁₀</code>: <code>X = 0, 1, 2</code> and <code>Y = 0, 2, 4</code>. Y climbs 2 for every 1 that X climbs.
      <br><b>4 · Sanity check:</b> slope <code>= (4 − 0) / (2 − 0) = 2</code>, exactly the exponent <code>b = 2</code> you started with. If the slope doesn't match the power you put in, you forgot to <code>log</code> one of the axes.</div></div>

      <h4>Why a frontier lab cares</h4>
      <p>Kaplan et al.'s 2020 scaling-law paper found loss falls with compute at roughly <code>α ≈ 0.05</code> — plot loss vs. compute on log-log axes and you get a nearly straight line with that slope. That's exactly the method behind Chinchilla's 20-tokens-per-parameter finding from the teaser above. Two years later, DeepMind used that same log-log approach on compute-optimal training, and found that tokens and parameters should grow together, not just model size alone. That one exponent — the slope you're about to read off a log-log plot — predicts how much better a model gets for every 10× more compute, which is the business case for spending millions on a training run.</p>
      <div class="callout c-info"><span class="ic">🔗</span><div><b>Remember this chain:</b> loss vs compute is a power law → on log-log it's a straight line → the slope is the scaling exponent → the exponent predicts the payoff of more compute. You'll <em>derive</em> these laws in the scaling modules; today you learned to read the axes.</div></div>

      <h4>The staff lens — one silent failure, one trade-off</h4>
      <p>Logs and exponents are also how we keep very small and very large numbers from breaking the arithmetic — and getting that wrong fails quietly.</p>
      <div class="callout c-warn"><span class="ic">⚠️</span><div><b>Failure mode (silent): underflow to zero, then <code>log(0)</code> (a bug that doesn't announce itself).</b> A model's probability for one token might be <code>0.001</code>. Multiply a few hundred of those together — as you would to score a sentence — and the product falls below the smallest number the dtype can hold, so it rounds to <em>exactly</em> <code>0.0</code>. No error fires. Then <code>log(0.0)</code> returns <code>-inf</code>, and one <code>-inf</code> poisons the average into <code>nan</code>, which quietly makes the whole loss useless. The fix, which a senior engineer looks for in <b>code review</b>, is to add the logs instead of logging the product: <code>log(a) + log(b) + ...</code> stays in a safe range because it never forms the tiny product at all. Seeing a raw <code>np.log(product_of_probs)</code> is the red flag.</div></div>
      <div class="callout c-info"><span class="ic">⚖️</span><div><b>Trade-off: work in log-space for safety vs. remembering the numbers changed meaning.</b> Moving to logs turns underflow-prone multiplication into safe addition (<code>log(a·b) = log a + log b</code>) and squashes a huge range into a small one. What you pay: your values are no longer probabilities or counts — they are log-units, so you must combine them by adding (not multiplying) and exponentiate only at the very end to read a real number back. In a <b>design review</b> this is the choice to keep an entire pipeline in log-space: safer arithmetic, but every step must respect that the numbers are logs.</div></div>
      <div class="callout c-ok"><span class="ic">🎤</span><div><b>Say this in an interview:</b> "A scaling law is a power law — <code>loss ∝ compute^(−α)</code> — so on log-log axes it's a straight line and the slope is the exponent. That one number tells you how much loss you buy per 10× more compute, which is the case for spending millions on a run. The nuance: do the arithmetic in log-space — <em>add</em> log-probabilities instead of logging a product — or a long sequence quietly underflows to <code>log(0) = −inf</code>."</div></div>
      <button class="gotit" type="button">Got the derivation</button>
    </div>
</section>

    <!-- ===== 5. BUILD IT UP ===== -->
    <section class="module-section" id="s5" data-sec="build">
  <div class="sec-head"><span class="sec-num s-code">5</span><span class="sec-h"><span class="lang-en">From an exponent to a scaling-law line — scroll, don't click</span><span class="lang-zh">从一个 exponent 到一条 scaling-law 直线 —— 往下滚，不用点</span></span><span class="sec-tag"><span class="lang-en">Build it up</span><span class="lang-zh">一步步搭起来</span></span></div>
  <div class="sec-body">
      <p><span class="lang-en">Here is the whole idea, built one step at a time. <strong>Scroll down slowly</strong> — each piece fades in with a short note. By the bottom you'll read any log-log line.</span><span class="lang-zh">整个想法都在下面，一步一步搭起来。<strong>慢慢往下滚</strong>。每一块都会淡淡地出现，旁边还有一句说明。滚到最下面，你就能读懂任何一条 log-log 直线了。</span></p>
      <div class="build-hint"><span class="lang-en">↓ scroll to reveal each piece ↓</span><span class="lang-zh">↓ 往下滚，一块一块出现 ↓</span></div>
      <div class="build" id="build"></div>
      <button class="gotit" type="button" disabled><span class="lang-en">scroll through the build first</span><span class="lang-zh">先把搭建过程滚完</span></button>
    </div>
</section>

    <!-- ===== 6. QUIZ ===== -->
    <section class="module-section" id="s6" data-sec="quiz">
  <div class="sec-head"><span class="sec-num s-quiz">6</span><span class="sec-h"><span class="lang-en">Click an answer — instant feedback on each</span><span class="lang-zh">点一个答案 —— 每题都会立刻给你反馈</span></span><span class="sec-tag"><span class="lang-en">Quiz</span><span class="lang-zh">小测</span></span></div>
  <div class="sec-body">
      <p><span class="lang-en">4 questions, instant feedback.</span><span class="lang-zh">四道题，每题立刻给反馈。</span> <strong><span class="lang-en">Answer all four</span><span class="lang-zh">四题都答完</span></strong> <span class="lang-en">to complete today.</span><span class="lang-zh">就算今天完成。</span></p>
      <div id="quiz"></div>
      <button class="gotit" type="button" disabled><span class="lang-en">answer all four first</span><span class="lang-zh">先把四题都答完</span></button>
    </div>
</section>

    <!-- ===== 7. PRODUCE ===== -->
    <section class="module-section" id="s7" data-sec="produce">
  <div class="sec-head"><span class="sec-num s-produce">7</span><span class="sec-h">Leave today's evidence (strongly recommended)</span><span class="sec-tag">Produce</span></div>
  <div class="sec-body">
      <p>Understanding ≠ being able to write it. Turn a power law into a line yourself — that's what makes the day count. Pick one:</p>
      <h4>Acceptance criteria</h4>
      <ul>
        <li><code>np.log(np.exp(3))</code> prints <code>3.0</code> — log and exp cancel.</li>
        <li><code>np.log10([1,10,100,1000])</code> prints <code>[0,1,2,3]</code> — each ×10 adds exactly 1.</li>
        <li>For <code>x=[1,10,100]</code>, <code>y=x**2</code>: <code>np.log10(y)</code> rises by 2 for every 1 that <code>np.log10(x)</code> rises, and the computed slope equals the exponent <code>2</code>.</li>
        <li>Runs cleanly with <code>python3 sessions/m01-shape-of-data/day-05-logs-and-exponents/experiment.py</code> and prints those three checks.</li>
      </ul>
      <h4>Option A · write it yourself</h4>
      <p>Create <code>sessions/m01-shape-of-data/day-05-logs-and-exponents/experiment.py</code>. Show that <code>np.log(np.exp(3))</code> is 3; print <code>np.log10([1,10,100,1000])</code>; then make <code>x = [1,10,100]</code>, <code>y = x**2</code>, and print <code>np.log10(x)</code> and <code>np.log10(y)</code> — check the log values rise by 2 for every 1 (slope 2). Run it with <code>python3 sessions/m01-shape-of-data/day-05-logs-and-exponents/experiment.py</code>.</p>
      <h4>Option B · let Claude build it, then read it</h4>
      <p>Copy the prompt below back into Claude Code. It has Claude Code create the file, write the code, and run it for you.</p>
      <div class="prompt">
        <div class="prompt-h"><span class="prompt-l">ask Claude to build it</span><button class="copy" type="button" data-copy="#pp"><span class="lang-en">📋 copy</span><span class="lang-zh">📋 复制</span></button></div>
        <pre class="prompt-t" id="pp">Help me build my Module 1 Day 5 artifact.

Create sessions/m01-shape-of-data/day-05-logs-and-exponents/experiment.py that, with a comment on each step:
1. Prints 2**3, np.exp(1), and np.log(np.exp(3)) to show exponent vs log and that they undo each other.
2. Prints np.log10([1,10,100,1000]) → [0,1,2,3], noting each ×10 adds 1 (orders of magnitude).
3. Builds x=np.array([1,10,100]); y=x**2; prints y, then np.log10(x) and np.log10(y); computes the slope (rise/run) of log10(y) vs log10(x) and asserts it equals 2 (the exponent).
Then run it and paste the output at the bottom as a comment.</pre>
      </div>
      <div class="callout c-info"><span class="ic">📓</span><div><b>5-minute research log · 5 分钟研究笔记:</b> 关掉页面前，用自己的话写三行 — before you close the tab, write three lines in <code>sessions/m01-shape-of-data/day-05-logs-and-exponents/log.md</code>: (1) in your own words, why does a power law become a straight line on log-log axes? (2) the number or output that surprised you; (3) one thing you're still unsure about.</div></div>
      <button class="gotit" type="button">Done</button>
    </div>
</section>

    <div class="fin" id="fin" role="status" aria-hidden="true">
      <span class="em" aria-hidden="true">🎉</span>
      <h3>Module 1 · Day 5 complete! 🏆</h3>
      <p>You can now read a log-log scaling plot: a power law becomes a straight line, and its slope is the exponent that predicts the payoff of more compute.<br>Next up: <b>Random Seeds &amp; PRNG</b> — make "random" repeat on command.</p>
    </div>

    <footer class="site-footer">Frontier Lab · Foundations — a fully self-contained single file. Progress is saved locally in your browser (localStorage), works offline, just double-click to open.</footer>

  </main>
</div>

<script>
(function(){
"use strict";
var QID = document.body.getAttribute('data-quest-id') || 'quest';
var KEY = 'frontier-lesson:'+QID;
var state = load();
function load(){ try{ return JSON.parse(localStorage.getItem(KEY))||{done:{}} }catch(e){ return {done:{}} } }
function save(){ try{ localStorage.setItem(KEY, JSON.stringify(state)) }catch(e){} }

var secs = Array.prototype.slice.call(document.querySelectorAll('.module-section'));
var bar = document.getElementById('bar') || document.getElementById('progress-bar');
var count = document.getElementById('count'), fin = document.getElementById('fin');
var RM = !!(window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches);
var navLinks = Array.prototype.slice.call(document.querySelectorAll('.nav-link'));

/* ---- build the sidebar checklist from sections ---- */
/* frontier-lang:checklist */
var checklist = document.getElementById('checklist'), checkItems = {}, _lblLang = null;
function secLabel(sec){
  /* A .sec-h can hold a paired <span class="lang-en">/<span class="lang-zh">. Reading
     the whole node's textContent would splice BOTH languages into one label, e.g.
     "The bend这个弯" — display:none hides a span visually but textContent still
     returns it. So read the one span for the current language, falling back to
     English, and to the whole node on a page with no pairs at all. */
  var h = sec.querySelector('.sec-h');
  if(!h) return sec.getAttribute('data-sec');
  var cur = document.documentElement.getAttribute('data-lang') || 'en';
  return (h.querySelector('.lang-'+cur) || h.querySelector('.lang-en') || h).textContent;
}
function buildChecklist(){
  /* Returns true when it actually rebuilt, so the caller knows whether to
     refresh. The language check lives HERE rather than in setLang because
     setLang's text is shared verbatim with pages that have no checklist at all
     (index.html, roadmap.html) — referencing _lblLang from there would throw. */
  if(!checklist) return false;
  var cur = document.documentElement.getAttribute('data-lang') || 'en';
  if(_lblLang === cur && checklist.children.length) return false;
  checklist.innerHTML = ''; checkItems = {};
  secs.forEach(function(sec){
    var key = sec.getAttribute('data-sec');
    var num = sec.querySelector('.sec-num') ? sec.querySelector('.sec-num').textContent : '';
    var li = document.createElement('li');
    li.innerHTML = '<span class="box" aria-hidden="true"></span><span>'+num+' · '+shorten(secLabel(sec))+'</span>';
    li.addEventListener('click', function(){ sec.scrollIntoView({behavior:RM?'auto':'smooth', block:'start'}); });
    checklist.appendChild(li);
    checkItems[key] = li;
  });
  _lblLang = cur;
  return true;
}
buildChecklist();
/* /frontier-lang:checklist */

function shorten(t){ return t.length>34 ? t.slice(0,32).trim()+'…' : t; }

function markDone(sec, scrollNext){
  sec.classList.add('done'); state.done[sec.getAttribute('data-sec')]=true; save(); refresh();
  if(scrollNext){
    var i = secs.indexOf(sec);
    if(i>-1 && i+1<secs.length) setTimeout(function(){ secs[i+1].scrollIntoView({behavior:RM?'auto':'smooth',block:'start'}); },180);
    else setTimeout(function(){ fin.scrollIntoView({behavior:RM?'auto':'smooth',block:'center'}); },180);
  }
}
secs.forEach(function(sec){
  if(state.done[sec.getAttribute('data-sec')]) sec.classList.add('done');
  var btn = sec.querySelector('.gotit');
  if(btn) btn.addEventListener('click', function(){ markDone(sec, true); });
});
function refresh(){
  var total = secs.length, done = secs.filter(function(s){return s.classList.contains('done')}).length;
  var pct = Math.round(done/total*100);
  if(bar){ bar.style.width = pct+'%'; bar.setAttribute('aria-valuenow', String(pct)); bar.setAttribute('aria-valuetext', pct+'% complete'); }
  if(count) count.innerHTML = '<b>'+done+'</b>/'+total+ui('sections_done');
  secs.forEach(function(s){
    var k = s.getAttribute('data-sec'), on = s.classList.contains('done');
    if(checkItems[k]) checkItems[k].classList.toggle('done', on);
    var nl = navLinks.filter(function(l){return l.getAttribute('data-target')===s.id;})[0];
    if(nl) nl.classList.toggle('done', on);
  });
  var all = done===total; fin.classList.toggle('show', all); fin.setAttribute('aria-hidden', all?'false':'true');
}
var resetBtn = document.getElementById('reset');
if(resetBtn) resetBtn.addEventListener('click', function(){
  if(!confirm(ui('reset_confirm'))) return;
  state={done:{}}; save(); secs.forEach(function(s){s.classList.remove('done')}); refresh();
});

/* ---- darkness switcher ---- */
var themeBtns = Array.prototype.slice.call(document.querySelectorAll('.theme-btn'));
function setTheme(t){
  document.documentElement.setAttribute('data-theme', t);
  try{ localStorage.setItem('frontier-theme', t); }catch(e){}
  themeBtns.forEach(function(b){ b.classList.toggle('active', b.getAttribute('data-theme-set')===t); });
}
themeBtns.forEach(function(b){ b.addEventListener('click', function(){ setTheme(b.getAttribute('data-theme-set')); }); });
setTheme(document.documentElement.getAttribute('data-theme') || 'dim');

/* frontier-lang:ui */
/* Runtime UI strings. These are the ones no CSS toggle can reach, because the code
   REPLACES textContent rather than showing one of two nodes — so in Chinese mode the
   page would flip back to English the moment the reader pressed anything. Kept as a
   table with an ui() lookup so a new string is one line in two places, not a new
   mechanism. Anything rendered ONCE and never rewritten stays a paired span in the
   markup instead; this table is only for text that JavaScript writes. */
var UI = {
  en: {reveal_done:'ran \u2713', all_answered:'All answered \u2014 check \u2713',
       hints_end:"\u2014 that's all the hints \u2014",
       hint_more:'\ud83d\udca1 still stuck? another hint',
       copied:'\u2713 copied', copy_manual:'select & copy manually',
       reset_confirm:"Reset today's progress?", sections_done:' sections done', saw_all_three:'Saw all three — got it ✓', saw_build:'Saw the whole build — got it ✓', answered_four:'All four answered — check ✓', wrong_prefix:'The correct answer is the green one. '},
  zh: {reveal_done:'\u5df2\u5c55\u5f00 \u2713', all_answered:'\u5168\u90e8\u4f5c\u7b54 \u2014 \u770b\u7ed3\u679c \u2713',
       hints_end:'\u2014 \u63d0\u793a\u5c31\u8fd9\u4e48\u591a\u4e86 \u2014',
       hint_more:'\ud83d\udca1 \u8fd8\u662f\u5361\u4f4f\uff1f\u518d\u770b\u4e00\u6761\u63d0\u793a',
       copied:'\u2713 \u5df2\u590d\u5236', copy_manual:'\u8bf7\u624b\u52a8\u9009\u4e2d\u590d\u5236',
       reset_confirm:'\u8981\u6e05\u7a7a\u4eca\u5929\u7684\u8fdb\u5ea6\u5417\uff1f', sections_done:' \u5c0f\u8282\u5b8c\u6210', saw_all_three:'三个都看了 —— 明白了 ✓', saw_build:'整个搭建过程都看了 —— 明白了 ✓', answered_four:'四题都答完了 —— 看结果 ✓', wrong_prefix:'正确答案是绿色那个。'}
};
function ui(k){
  var l = document.documentElement.getAttribute('data-lang');
  return (UI[l] && UI[l][k] !== undefined) ? UI[l][k] : UI.en[k];
}
/* /frontier-lang:ui */

/* frontier-lang:controller */
/* ---- reading-language switcher (English / 中文) ---- */
/* Mirrors setTheme: flip an attribute on <html>, let CSS do the rest, remember the
   choice. Two things it must get right that setTheme does not have to:
     * <html lang> moves too, not just data-lang. A screen reader on a page marked
       lang="en" reads CJK with an English voice, which is worse than not
       translating it at all.
     * a page with no Chinese content must NOT persist a forced fallback. Without
       that guard, opening one untranslated day would silently reset the reader's
       language for the whole site. */
var langBtns = Array.prototype.slice.call(document.querySelectorAll('.lang-btn'));
var hasZh = !!document.querySelector('.lang-zh');
function setLang(l, persist){
  if(l==='zh' && !hasZh){ l='en'; persist=false; }
  document.documentElement.setAttribute('data-lang', l);
  document.documentElement.setAttribute('lang', l==='zh' ? 'zh-Hans' : 'en');
  if(persist!==false){ try{ localStorage.setItem('frontier-lang', l); }catch(e){} }
  langBtns.forEach(function(b){ b.classList.toggle('active', b.getAttribute('data-lang-set')===l); });
  /* Checklist labels are language-dependent. Both calls are guarded by typeof so
     this exact block can also be pasted onto pages that have no checklist and no
     refresh() at all (the hub and the roadmap) — one shared text, no variants. */
  /* A ui() string already stamped into the DOM keeps the language it was
     written in unless we re-stamp it. Verified defect: reveal a demo in
     English, press 中文, and the button still read 'ran \u2713'. */
  Array.prototype.slice.call(document.querySelectorAll('[data-ui]')).forEach(
    function(el){ el.textContent = ui(el.getAttribute('data-ui')); });
  if(typeof buildChecklist === 'function' && buildChecklist()){
    if(typeof refresh === 'function') refresh();
  }
}
langBtns.forEach(function(b){
  if(b.getAttribute('data-lang-set')==='zh' && !hasZh){
    b.setAttribute('aria-disabled','true');
    b.setAttribute('title','中文版尚未提供 — no Chinese on this page yet');
  }
  b.addEventListener('click', function(){ setLang(b.getAttribute('data-lang-set')); });
});
setLang(document.documentElement.getAttribute('data-lang') || 'en', false);
/* /frontier-lang:controller */

/* ---- sidebar nav: click to scroll, scrollspy to highlight active ---- */
navLinks.forEach(function(l){
  l.addEventListener('click', function(){
    var t = document.getElementById(l.getAttribute('data-target'));
    if(t) t.scrollIntoView({behavior:RM?'auto':'smooth', block:'start'});
    setTimeout(function(){ if(window.__revealBuild) window.__revealBuild(); }, RM?60:650);
    closeSidebar();
  });
});
var spyTargets = [document.getElementById('home')].concat(secs).filter(Boolean);
if('IntersectionObserver' in window){
  var spy = new IntersectionObserver(function(es){
    es.forEach(function(e){
      if(e.isIntersecting){
        var id = e.target.id;
        navLinks.forEach(function(l){ l.classList.toggle('active', l.getAttribute('data-target')===id); });
      }
    });
  }, {threshold:0.01, rootMargin:'-45% 0px -50% 0px'});
  spyTargets.forEach(function(t){ spy.observe(t); });
}

/* ---- mobile sidebar toggle ---- */
var sidebar = document.getElementById('sidebar'), toggle = document.getElementById('nav-toggle'), scrim = document.getElementById('scrim');
function openSidebar(){ sidebar.classList.add('open'); scrim.classList.add('show'); toggle.setAttribute('aria-expanded','true'); }
function closeSidebar(){ sidebar.classList.remove('open'); scrim.classList.remove('show'); toggle.setAttribute('aria-expanded','false'); }
if(toggle) toggle.addEventListener('click', function(){ sidebar.classList.contains('open')?closeSidebar():openSidebar(); });
if(scrim) scrim.addEventListener('click', closeSidebar);

/* ===== PLAYGROUND ===== */
var DEMOS = {
  explog:{html:'<span class="prompt">&gt;&gt;&gt;</span> <span class="num">2</span> ** <span class="num">3</span>            <span class="dim"># exponent: 2 multiplied 3 times</span>\n'+
    '<span class="hl">8</span>\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> np.exp(<span class="num">1</span>)         <span class="dim"># e to the power 1</span>\n'+
    '<span class="ok">2.718281828459045</span>\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> np.log(np.exp(<span class="num">3</span>))  <span class="dim"># log undoes exp</span>\n'+
    '<span class="ok">3.0</span>',
    take:'<b>①  Log and exp are inverses.</b> An exponent multiplies; a log asks "to what power?" So <code>log(exp(3))</code> gives back 3 — they cancel, like + and −.'},
  oom:{html:'<span class="prompt">&gt;&gt;&gt;</span> np.log10([<span class="num">1</span>, <span class="num">10</span>, <span class="num">100</span>, <span class="num">1000</span>])\n'+
    '<span class="hl">array([0., 1., 2., 3.])</span>',
    take:'<b>②  log10 counts orders of magnitude.</b> Each ×10 adds exactly 1 to the log. That is why 1 all the way to a billion can sit, evenly spaced, on a single axis.'},
  power:{html:'<span class="prompt">&gt;&gt;&gt;</span> x = np.array([<span class="num">1</span>, <span class="num">10</span>, <span class="num">100</span>])\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> y = x ** <span class="num">2</span>              <span class="dim"># a power law: y = x^2</span>\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> y\n<span class="ok">array([    1,   100, 10000])</span>\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> np.log10(x)\n<span class="hl">array([0., 1., 2.])</span>\n'+
    '<span class="prompt">&gt;&gt;&gt;</span> np.log10(y)\n<span class="hl">array([0., 2., 4.])</span>',
    take:'<b>③  The trick, in numbers.</b> In log space the points are (0,0), (1,2), (2,4) — a perfectly straight line with <b>slope 2</b>. The exponent (2) became the slope. That is how you read a scaling law.'}
};
var playSec=document.getElementById('s3'), consoleEl=document.getElementById('console'), takeEl=document.getElementById('take');
var playBtns=Array.prototype.slice.call(playSec.querySelectorAll('.play-btn')), playSeen={};
playBtns.forEach(function(b){ b.addEventListener('click', function(){
  var k=b.getAttribute('data-demo');
  playBtns.forEach(function(o){o.classList.remove('active')}); b.classList.add('active'); b.classList.add('seen');
  consoleEl.innerHTML=DEMOS[k].html; takeEl.innerHTML=DEMOS[k].take; playSeen[k]=true;
  if(Object.keys(playSeen).length>=3){ var g=playSec.querySelector('.gotit'); g.disabled=false; g.setAttribute('data-ui','saw_all_three');g.textContent=ui('saw_all_three'); }
}); });

/* ===== BUILD — scroll-reveal ===== */
var BUILD=[
 {viz:"<svg viewBox='0 0 520 110' role='img' aria-label='Exponent 2 to the 3 equals 8'><g font-family='monospace' font-size='15' text-anchor='middle'><text x='120' y='55' fill='#1F6280'>2³ = 2·2·2</text><text x='250' y='55' fill='#6B645E'>=</text><rect x='285' y='36' width='44' height='34' rx='6' fill='#E8F5EE' stroke='#2D8B55' stroke-width='2'/><text x='307' y='59' fill='#1a5c38' font-weight='bold'>8</text><text x='260' y='92' fill='#6B645E' font-size='12'>exponent = how many times you multiply</text></g></svg>",
  note:"<b>Start with an exponent.</b> <code>2³</code> means multiply 2 by itself three times → 8. The little number on top is just a count of multiplications."},
 {viz:"<svg viewBox='0 0 520 110' role='img' aria-label='Log undoes exponent: log2 of 8 is 3, log10 of 1000 is 3'><g font-family='monospace' font-size='15' text-anchor='middle'><text x='130' y='48' fill='#9A7208'>log₂(8) = 3</text><text x='130' y='76' fill='#9A7208'>log₁₀(1000) = 3</text><text x='300' y='62' fill='#6B645E'>←→</text><text x='400' y='62' fill='#1F6280' font-size='12'>the inverse of ^</text></g></svg>",
  note:"<b>A log undoes an exponent.</b> It asks 'to what power?': <code>log₂(8)=3</code> because <code>2³=8</code>; <code>log₁₀(1000)=3</code> because <code>10³=1000</code>. Log and exponent are opposites."},
 {viz:"<svg viewBox='0 0 520 110' role='img' aria-label='On a log axis, 1 10 100 1000 are evenly spaced'><g font-family='monospace' font-size='13' text-anchor='middle'><line x1='40' y1='60' x2='480' y2='60' stroke='#5A544E' stroke-width='1.5'/><g fill='#5E5191'><circle cx='70' cy='60' r='4'/><circle cx='200' cy='60' r='4'/><circle cx='330' cy='60' r='4'/><circle cx='460' cy='60' r='4'/></g><text x='70' y='82' fill='#2C2A28'>1</text><text x='200' y='82' fill='#2C2A28'>10</text><text x='330' y='82' fill='#2C2A28'>100</text><text x='460' y='82' fill='#2C2A28'>1000</text><text x='135' y='48' fill='#9A7208' font-size='11'>×10</text><text x='265' y='48' fill='#9A7208' font-size='11'>×10</text><text x='395' y='48' fill='#9A7208' font-size='11'>×10</text></g></svg>",
  note:"<b>A log axis spaces things by ×10.</b> 1, 10, 100, 1000 sit at equal distances — each gap is one order of magnitude. That is how a range from 1 to a billion fits on one screen."},
 {viz:"<svg viewBox='0 0 520 140' role='img' aria-label='y = x squared drawn on normal axes is a rising curve'><g font-family='monospace'><line x1='60' y1='120' x2='60' y2='20' stroke='#5A544E' stroke-width='1.5'/><line x1='60' y1='120' x2='460' y2='120' stroke='#5A544E' stroke-width='1.5'/><path d='M60 120 Q 300 118 300 60 Q 300 25 440 25' fill='none' stroke='#C93B3B' stroke-width='2.5'/><text x='250' y='138' text-anchor='middle' font-size='12' fill='#2C2A28'>x  (normal axis)</text><text x='250' y='45' text-anchor='middle' font-size='12' fill='#C93B3B'>y = x²  →  a curve</text></g></svg>",
  note:"<b>On normal axes, a power law bends.</b> <code>y = x²</code> shoots upward as a curve — hard to read, hard to compare, hard to fit a simple rule to."},
 {viz:"<svg viewBox='0 0 520 150' role='img' aria-label='On log-log axes the same y = x squared is a straight line with slope 2'><g font-family='monospace'><line x1='60' y1='125' x2='60' y2='20' stroke='#5A544E' stroke-width='1.5'/><line x1='60' y1='125' x2='460' y2='125' stroke='#5A544E' stroke-width='1.5'/><line x1='80' y1='120' x2='420' y2='35' stroke='#2D8B55' stroke-width='2.5'/><circle cx='80' cy='120' r='4' fill='#2D8B55'/><circle cx='250' cy='77' r='4' fill='#2D8B55'/><circle cx='420' cy='35' r='4' fill='#2D8B55'/><text x='250' y='143' text-anchor='middle' font-size='12' fill='#2C2A28'>log x</text><text x='30' y='75' text-anchor='middle' font-size='12' fill='#2C2A28' transform='rotate(-90 30 75)'>log y</text><text x='330' y='58' font-size='12' fill='#2D8B55'>a straight line!</text></g></svg>",
  note:"<b>On log-log axes, it straightens.</b> Take the log of both x and y and the same <code>y = x²</code> data falls on a perfectly straight line. This is the single most useful plot type in ML."},
 {viz:"<svg viewBox='0 0 520 160' role='img' aria-label='The slope of the log-log line equals the exponent; scaling law reads loss vs compute with slope alpha'><g font-family='monospace'><line x1='60' y1='120' x2='60' y2='25' stroke='#5A544E' stroke-width='1.5'/><line x1='60' y1='120' x2='440' y2='120' stroke='#5A544E' stroke-width='1.5'/><line x1='80' y1='40' x2='420' y2='110' stroke='#2A7B9B' stroke-width='2.5'/><text x='250' y='60' font-size='12' fill='#2A7B9B'>slope = the exponent</text><text x='250' y='140' text-anchor='middle' font-size='11.5' fill='#2C2A28'>log(compute) →</text><text x='30' y='75' text-anchor='middle' font-size='11.5' fill='#2C2A28' transform='rotate(-90 30 75)'>log(loss)</text><text x='250' y='156' text-anchor='middle' font-size='10.5' fill='#6B645E'>a scaling law: loss falls as a power of compute</text></g></svg>",
  note:"<b>Why it runs the industry.</b> Scaling laws plot loss vs compute on log-log axes; the line's <b>slope is the exponent</b> that predicts how much loss drops per 10× more compute. Read that slope and you've read the chart every frontier lab bets millions on. That closes Module 1 — you now speak the language."}
];
var buildWrap=document.getElementById('build'), buildSec=document.getElementById('s5');
BUILD.forEach(function(item,i){
  var step=document.createElement('div'); step.className='build-step';
  step.innerHTML='<div class="build-viz">'+item.viz+'</div><div class="build-note"><span class="build-num">'+(i+1)+'</span>'+item.note+'</div>';
  buildWrap.appendChild(step);
});
var bsteps=Array.prototype.slice.call(buildWrap.querySelectorAll('.build-step')), bseen=0;
function bUnlock(){ var g=buildSec.querySelector('.gotit'); if(g){ g.disabled=false; g.setAttribute('data-ui','saw_build');g.textContent=ui('saw_build'); } }
function seeStep(s){ if(s.classList.contains('revealed'))return; s.classList.add('revealed'); bseen++; if(bseen>=bsteps.length) bUnlock(); }
function revealVisible(){
  var vh = window.innerHeight || document.documentElement.clientHeight;
  bsteps.forEach(function(s){
    if(s.classList.contains('revealed')) return;
    // reveal any step whose top has entered (or scrolled above) the viewport — never leave it blank
    if(s.getBoundingClientRect().top < vh*0.95) seeStep(s);
  });
}
window.__revealBuild = revealVisible;   // sidebar jumps re-check so a jumped-to build is never blank
if(!bsteps.length){ bUnlock(); }
else if(RM || !('IntersectionObserver' in window)){ bsteps.forEach(seeStep); }
else {
  buildWrap.classList.add('armed');
  var bio=new IntersectionObserver(function(es){ es.forEach(function(e){ if(e.isIntersecting){ seeStep(e.target); bio.unobserve(e.target); } }); }, {threshold:0.15, rootMargin:'0px 0px -6% 0px'});
  bsteps.forEach(function(s){ bio.observe(s); });
  // robustness: reveal steps already on screen (e.g. after a sidebar jump) and on any scroll/resize
  window.addEventListener('scroll', revealVisible, {passive:true});
  window.addEventListener('resize', revealVisible);
  revealVisible();
  setTimeout(revealVisible, 700);
}

/* ===== QUIZ ===== */
var QS=[
 {q:'1. A log undoes which operation?',
  opts:['Addition','Exponentiation (raising to a power)','Matrix multiply','Slicing'],
  ans:1, fb:'Right. log and exp are inverses: log(exp(x)) = x, just like + and − cancel.'},
 {q:'2. On a log-log plot, a power law <code>y = a·x^b</code> looks like:',
  opts:['A curve that bends upward','A straight line','A circle','A flat horizontal line'],
  ans:1, fb:'Right. Taking logs turns y = a·x^b into log y = log a + b·log x — a straight line.'},
 {q:'3. For <code>y = a·x^b</code>, the slope of that log-log line equals:',
  opts:['a','b (the exponent)','log a','1'],
  ans:1, fb:'Right. The slope is b — the exponent. The intercept is log a.'},
 {q:'4. Your sentence-scoring code multiplies a few hundred per-token probabilities together and then takes <code>np.log</code> of the product. It runs with no error, but the score comes out as <code>-inf</code> or <code>nan</code> for long sentences only. Where do you look first, and why?',
  opts:['The log function is broken and should be replaced','The product underflowed to exactly 0.0 (many small numbers multiplied), so <code>log(0.0)</code> is <code>-inf</code>; add the logs instead of logging the product','The sentences are simply too rare, so nan is correct','The exponent slope is negative, which always yields nan'],
  ans:1, fb:'Right. Multiplying hundreds of values below 1 drives the product under the smallest representable number, so it rounds to <code>0.0</code> with no warning; <code>log(0.0) = -inf</code> then poisons the average into <code>nan</code>. Only long sentences hit it because they have more factors. The fix is to sum the logs — <code>log(a)+log(b)+...</code> — which never forms the tiny product.'}
];
var quizWrap=document.getElementById('quiz'), quizSec=document.getElementById('s6'), answered={};
QS.forEach(function(item,qi){
  var block=document.createElement('div'); block.className='q';
  var ask=document.createElement('div'); ask.className='q-ask'; ask.innerHTML=item.q; block.appendChild(ask);
  var opts=document.createElement('div'); opts.className='q-opts';
  var fb=document.createElement('div'); fb.className='q-fb';
  item.opts.forEach(function(text,oi){
    var o=document.createElement('button'); o.type='button'; o.className='q-opt';
    o.innerHTML='<span class="mark"></span><span>'+text+'</span>';
    o.addEventListener('click', function(){
      if(answered[qi]) return; answered[qi]=true;
      Array.prototype.slice.call(opts.children).forEach(function(c){c.classList.add('locked')});
      if(oi===item.ans){ o.classList.add('correct'); fb.className='q-fb good show'; fb.innerHTML='✓ '+item.fb; }
      else { o.classList.add('wrong'); opts.children[item.ans].classList.add('correct'); fb.className='q-fb bad show'; fb.innerHTML='<span data-ui="wrong_prefix">'+ui('wrong_prefix')+'</span>'+item.fb; }
      if(Object.keys(answered).length>=QS.length){ var g=quizSec.querySelector('.gotit'); g.disabled=false; g.setAttribute('data-ui','answered_four');g.textContent=ui('answered_four'); }
    });
    opts.appendChild(o);
  });
  block.appendChild(opts); block.appendChild(fb); quizWrap.appendChild(block);
});

/* ===== COPY ===== */
Array.prototype.slice.call(document.querySelectorAll('.copy')).forEach(function(btn){
  btn.addEventListener('click', function(){
    var t=document.querySelector(btn.getAttribute('data-copy')), txt=t?t.textContent.trim():'';
    var done=function(ok){ var o=btn.innerHTML; btn.textContent=ok?ui('copied'):ui('copy_manual'); btn.classList.toggle('done',ok); setTimeout(function(){btn.innerHTML=o;btn.classList.remove('done')},1600); };
    if(navigator.clipboard && window.isSecureContext) navigator.clipboard.writeText(txt).then(function(){done(true)},function(){legacy(txt,done)});
    else legacy(txt,done);
  });
});
function legacy(txt,cb){ try{ var ta=document.createElement('textarea'); ta.value=txt; ta.style.position='fixed'; ta.style.top='-1000px'; document.body.appendChild(ta); ta.select(); var ok=document.execCommand('copy'); document.body.removeChild(ta); cb(ok); }catch(e){ cb(false); } }

refresh();
})();
</script>

<!-- ── playground visual: power law → straight line on log-log (dependency-free SVG) ── -->
<script>
(function(){
  "use strict";
  var left=document.getElementById('lg-left'), right=document.getElementById('lg-right'), leftYR=document.getElementById('lg-left-yr');
  if(!left || !right) return;
  var W=460,H=300,padL=48,padR=14,padT=20,padB=34;
  var log10=function(v){ return Math.log(v)/Math.LN10; };
  var clamp=function(v,lo,hi){ return Math.max(lo,Math.min(hi,v)); };
  var DEC=[0,1,2,3]; // log10(x) at x = 1, 10, 100, 1000
  function sx(v,d0,d1){ return padL+(v-d0)/(d1-d0)*(W-padL-padR); }
  function sy(v,d0,d1){ return (H-padB)-(v-d0)/(d1-d0)*((H-padB)-padT); }
  function axes(xlab,ylab){
    return '<line class="vlab-axis" x1="'+padL+'" y1="'+(H-padB)+'" x2="'+(W-padR)+'" y2="'+(H-padB)+'"/>'
      +'<line class="vlab-axis" x1="'+padL+'" y1="'+padT+'" x2="'+padL+'" y2="'+(H-padB)+'"/>'
      +'<text class="vlab-lbl" x="'+((padL+W-padR)/2)+'" y="'+(H-4)+'" text-anchor="middle">'+xlab+'</text>'
      +'<text class="vlab-lbl" x="13" y="'+((padT+H-padB)/2)+'" text-anchor="middle" transform="rotate(-90 13 '+((padT+H-padB)/2)+')">'+ylab+'</text>';
  }
  function xticks(vals,d0,d1,fmt){ var s=''; vals.forEach(function(v){ var x=sx(v,d0,d1);
    s+='<line class="vlab-grid" x1="'+x.toFixed(1)+'" y1="'+padT+'" x2="'+x.toFixed(1)+'" y2="'+(H-padB)+'"/>'
      +'<text class="vlab-lbl mut" x="'+x.toFixed(1)+'" y="'+(H-padB+12)+'" text-anchor="middle">'+fmt(v)+'</text>'; }); return s; }
  function yticks(vals,d0,d1,fmt){ var s=''; vals.forEach(function(v){ var y=sy(v,d0,d1);
    s+='<line class="vlab-grid" x1="'+padL+'" y1="'+y.toFixed(1)+'" x2="'+(W-padR)+'" y2="'+y.toFixed(1)+'"/>'
      +'<text class="vlab-lbl mut" x="'+(padL-5)+'" y="'+(y+3).toFixed(1)+'" text-anchor="end">'+fmt(v)+'</text>'; }); return s; }
  function fmtY(v){ if(v===0) return '0'; var a=Math.abs(v); if(a>=1000||a<0.01) return v.toExponential(0); return (Math.round(v*100)/100).toString(); }
  // LEFT · normal axes: y = x^b (a=1), autoscaled — shows the curve bending
  function renderLeft(b){
    var xd0=0,xd1=1000,N=64,i,x,y,ymax=1e-9,pts=[],dots='';
    var ys=[]; for(i=0;i<=N;i++){ x=1+(1000-1)*i/N; y=Math.pow(x,b); ys.push(y); if(y>ymax)ymax=y; }
    var yd0=0,yd1=(ymax>0?ymax*1.10:1);
    if(leftYR) leftYR.textContent='y max ≈ '+fmtY(yd1)+' — watch this number explode as b grows';
    var s=xticks([0,500,1000],xd0,xd1,function(v){return v;})+yticks([yd1/2,yd1],yd0,yd1,fmtY)+axes('x  (normal axis)','y = a·x^b');
    for(i=0;i<=N;i++){ x=1+(1000-1)*i/N; pts.push(sx(x,xd0,xd1).toFixed(1)+' '+sy(clamp(ys[i],yd0,yd1),yd0,yd1).toFixed(1)); }
    s+='<path fill="none" stroke="#B0453A" stroke-width="2.4" d="M'+pts.join(' L')+'"/>';
    for(i=0;i<DEC.length;i++){ x=Math.pow(10,DEC[i]); y=Math.pow(x,b);
      dots+='<circle class="vlab-pt" cx="'+sx(x,xd0,xd1).toFixed(1)+'" cy="'+sy(clamp(y,yd0,yd1),yd0,yd1).toFixed(1)+'" r="3.4" style="fill:#B0453A"/>'; }
    s+=dots+'<text class="vlab-lbl" x="'+((padL+W-padR)/2)+'" y="'+(padT+11)+'" text-anchor="middle" style="fill:#B0453A">a curve — hard to read</text>';
    left.innerHTML=s;
  }
  // RIGHT · log-log (straight, slope=b) OR log-y-only (curved) — same endpoints, different path
  function renderRight(b,mode){
    var Yd0=-3.5,Yd1=9.5,i,s;
    if(mode==='loglog'){
      var Xd0=0,Xd1=3;
      s=xticks([0,1,2,3],Xd0,Xd1,function(v){return v;})+yticks([-3,0,3,6,9],Yd0,Yd1,function(v){return v;})+axes('log₁₀(x)','log₁₀(y)');
      s+='<line class="vlab-grid" x1="'+padL+'" y1="'+sy(0,Yd0,Yd1).toFixed(1)+'" x2="'+(W-padR)+'" y2="'+sy(0,Yd0,Yd1).toFixed(1)+'"/>';
      var yEnd=3*b;
      s+='<line class="vlab-fit" x1="'+sx(0,Xd0,Xd1).toFixed(1)+'" y1="'+sy(clamp(0,Yd0,Yd1),Yd0,Yd1).toFixed(1)+'" x2="'+sx(3,Xd0,Xd1).toFixed(1)+'" y2="'+sy(clamp(yEnd,Yd0,Yd1),Yd0,Yd1).toFixed(1)+'"/>';
      // slope triangle: run 1, rise b, between X=1 and X=2 (drawn only if it stays on-panel)
      var yr1=clamp(b*1,Yd0,Yd1), yr2=clamp(b*2,Yd0,Yd1);
      if(b*1>Yd0+0.2 && b*2<Yd1-0.2 && b*1<Yd1-0.2 && b*2>Yd0+0.2){
        s+='<line x1="'+sx(1,Xd0,Xd1).toFixed(1)+'" y1="'+sy(yr1,Yd0,Yd1).toFixed(1)+'" x2="'+sx(2,Xd0,Xd1).toFixed(1)+'" y2="'+sy(yr1,Yd0,Yd1).toFixed(1)+'" stroke="#2A7B9B" stroke-width="1.4" stroke-dasharray="4 3"/>';
        s+='<line x1="'+sx(2,Xd0,Xd1).toFixed(1)+'" y1="'+sy(yr1,Yd0,Yd1).toFixed(1)+'" x2="'+sx(2,Xd0,Xd1).toFixed(1)+'" y2="'+sy(yr2,Yd0,Yd1).toFixed(1)+'" stroke="#2A7B9B" stroke-width="1.4" stroke-dasharray="4 3"/>';
        s+='<text class="vlab-lbl" x="'+((sx(1,Xd0,Xd1)+sx(2,Xd0,Xd1))/2).toFixed(1)+'" y="'+(sy(yr1,Yd0,Yd1)+13).toFixed(1)+'" text-anchor="middle" style="fill:#2A7B9B">run = 1</text>';
        s+='<text class="vlab-lbl" x="'+(sx(2,Xd0,Xd1)+5).toFixed(1)+'" y="'+((sy(yr1,Yd0,Yd1)+sy(yr2,Yd0,Yd1))/2).toFixed(1)+'" style="fill:#2A7B9B">rise = b</text>';
      }
      for(i=0;i<DEC.length;i++){ s+='<circle class="vlab-pt" cx="'+sx(DEC[i],Xd0,Xd1).toFixed(1)+'" cy="'+sy(clamp(b*DEC[i],Yd0,Yd1),Yd0,Yd1).toFixed(1)+'" r="3.6" style="fill:#2D8B55"/>'; }
      s+='<text class="vlab-lbl" x="'+((padL+W-padR)/2)+'" y="'+(padT+11)+'" text-anchor="middle" style="fill:#2D8B55">a straight line — slope = b</text>';
    } else {
      var xd0=0,xd1=1000,N=64,pts=[],x,Y;
      s=xticks([0,500,1000],xd0,xd1,function(v){return v;})+yticks([-3,0,3,6,9],Yd0,Yd1,function(v){return v;})+axes('x  (linear axis)','log₁₀(y)');
      s+='<line class="vlab-grid" x1="'+padL+'" y1="'+sy(0,Yd0,Yd1).toFixed(1)+'" x2="'+(W-padR)+'" y2="'+sy(0,Yd0,Yd1).toFixed(1)+'"/>';
      for(i=0;i<=N;i++){ x=1+(1000-1)*i/N; Y=b*log10(x); pts.push(sx(x,xd0,xd1).toFixed(1)+' '+sy(clamp(Y,Yd0,Yd1),Yd0,Yd1).toFixed(1)); }
      s+='<path class="vlab-fit dash" d="M'+pts.join(' L')+'"/>';
      for(i=0;i<DEC.length;i++){ x=Math.pow(10,DEC[i]); Y=b*DEC[i]; s+='<circle class="vlab-pt" cx="'+sx(x,xd0,xd1).toFixed(1)+'" cy="'+sy(clamp(Y,Yd0,Yd1),Yd0,Yd1).toFixed(1)+'" r="3.6" style="fill:#B0453A"/>'; }
      s+='<text class="vlab-lbl" x="'+((padL+W-padR)/2)+'" y="'+(padT+11)+'" text-anchor="middle" style="fill:#B0453A">still a curve — a line misses it</text>';
    }
    right.innerHTML=s;
  }
  var slider=document.getElementById('lg-exp'), lbl=document.getElementById('lg-exp-lbl'), bout=document.getElementById('lg-b'), rcap=document.getElementById('lg-right-cap');
  var bBoth=document.getElementById('lg-both'), bSemi=document.getElementById('lg-semi'), mode='loglog';
  function update(){
    var b=parseFloat(slider.value); renderLeft(b); renderRight(b,mode);
    lbl.textContent='b = '+b.toFixed(2); bout.textContent=b.toFixed(2);
    rcap.textContent = mode==='loglog' ? 'Right · log-log axes — a straight line, slope = b' : 'Right · log y only — the same points stay curved';
    bBoth.classList.toggle('active',mode==='loglog'); bBoth.setAttribute('aria-pressed',mode==='loglog');
    bSemi.classList.toggle('active',mode==='semilog'); bSemi.setAttribute('aria-pressed',mode==='semilog');
  }
  slider.addEventListener('input',update);
  bBoth.addEventListener('click',function(){mode='loglog';update();});
  bSemi.addEventListener('click',function(){mode='semilog';update();});
  var p2=document.getElementById('lg-p2'), pk=document.getElementById('lg-pk');
  if(p2) p2.addEventListener('click',function(){slider.value='2';mode='loglog';update();});
  if(pk) pk.addEventListener('click',function(){slider.value='-0.05';mode='loglog';update();});
  update();
})();
</script>

<!-- ── glossary tooltips ── -->
<script>
(function(){
  "use strict";
  var terms = document.querySelectorAll('.term[data-tip]');
  if(!terms.length) return;
  var tip = document.createElement('div');
  tip.id = 'term-tip'; tip.setAttribute('role','tooltip');
  document.body.appendChild(tip);
  var active = null;
  function position(el){
    var r = el.getBoundingClientRect();
    tip.style.left = '0px'; tip.style.top = '0px';
    var t = tip.getBoundingClientRect(), pad = 8;
    var left = r.left + r.width/2 - t.width/2;
    left = Math.max(pad, Math.min(left, window.innerWidth - t.width - pad));
    var top = r.top - t.height - pad;
    if(top < pad) top = r.bottom + pad;
    tip.style.left = Math.round(left)+'px';
    tip.style.top  = Math.round(top)+'px';
  }
  function show(el){
    active = el; tip.textContent = '';
    var ttl = document.createElement('span'); ttl.className = 'ttl';
    ttl.textContent = (el.textContent||'').replace(/[.:,;]+$/,'').trim();
    var body = document.createElement('span'); body.textContent = el.getAttribute('data-tip');
    tip.appendChild(ttl); tip.appendChild(body); tip.classList.add('show'); position(el);
  }
  function hide(){ active = null; tip.classList.remove('show'); }
  Array.prototype.forEach.call(terms, function(el){
    if(!el.hasAttribute('tabindex')) el.setAttribute('tabindex','0');
    el.addEventListener('mouseenter', function(){ show(el); });
    el.addEventListener('mouseleave', hide);
    el.addEventListener('focus', function(){ show(el); });
    el.addEventListener('blur', hide);
  });
  window.addEventListener('scroll', function(){ if(active) position(active); }, {passive:true});
  window.addEventListener('resize', function(){ if(active) position(active); });
  document.addEventListener('keydown', function(e){ if(e.key==='Escape') hide(); });
})();
</script>

<!-- ── viz iframe: auto-size to content + manual reload safety net ──
     Root cause of the earlier "blank box" was Safari treating each local file://
     path as its own origin, so an iframe embedded from a *different* file gets its
     paint suppressed as a privacy measure — confirmed by: it worked standalone,
     it worked in Chrome, and it worked over http://localhost. Not a code bug, and
     it does not affect the deployed (https) site. Auto-resize restored; the reload
     button stays as a harmless safety net regardless of browser. -->
<script>
(function(){
  window.addEventListener('message', function(e){
    var d = e.data;
    if (!d || d.type !== 'viz-height' || typeof d.px !== 'number') return;
    var frames = document.querySelectorAll('.build-embed iframe');
    for (var i = 0; i < frames.length; i++){
      if (frames[i].contentWindow === e.source){
        frames[i].style.height = Math.min(Math.max(d.px, 320), 3200) + 'px';
        break;
      }
    }
  });
  var btn = document.getElementById('bcastReload');
  if(btn) btn.addEventListener('click', function(){
    var f = document.getElementById('bcastFrame');
    if(f) f.src = f.src;
  });
})();
</script>
</body>
</html>
