# Mock Interview System — backend dependencies
# All are already present in the repo's Python 3.11 environment; pinned loosely
# so a fresh machine can reproduce. Install with:
#   pip install -r interview_app/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
openai>=1.40
python-dotenv>=1.0
pydantic>=2.6
# Test-only
pytest>=8.0
